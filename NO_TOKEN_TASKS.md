# 진행 상태 — 백로그 100 (토큰 비용 기준 분류)

**이 문서가 백로그 100건의 상태 단일 출처다.** 각 번호가 무엇이고 왜 필요한지는
[BACKLOG.md](BACKLOG.md) 에 있다(그쪽에는 상태 열이 없다 — 두 곳에 상태를 두면 어긋난다).

묶음 기준: **MakeFun 이미지 생성 토큰(`MAKEFUN_API_TOKEN`)을 한 번도 쓰지 않고** 끝낼 수
있는가. 그래서 "지금 돈 안 쓰고 할 수 있는 것"이 바로 보인다.

**현황 — 95 / 100 완료.** 남은 5건은 토큰(21·38) 또는 그 결과물(22·27)에 묶여 있고,
1건(28)은 사용자 결정 대기다. 토큰 없이 가능한 작업은 **남은 게 없다.**
(**34** 는 완료됐다 — MakeFun 이 아니라 무료 로컬 ComfyUI 로 구웠다.)
이후 적대적 교차 검증에서 확인된 결함 **18건도 모두 수정**(대화 영구 손실·승인 이미지 교체·CSRF 등).
1차 렌더로 드러난 **렌더 품질 항목 101·102 도 완료**(다인물 컷이 한 명만 그려지던 문제).
실측으로 찾은 **프롬프트 레버와 화면의 진실 116～124 도 완료**(넓은 컷이 클로즈업으로 나오던 문제,
실패가 성공으로 보고되던 문제, 행동 한 번에 82.5MB 가 흐르던 문제).
**장면 12개 전부 APPROVED — 두 갈래 엔딩까지 그림이 들어갔다.**
감상본·스튜디오 뷰어를 실제 브라우저로 몰아본 **뷰어 런타임 107～112 도 완료**(선택지를 보지도 못한 채 분기가 정해지던 문제 포함).
완성 **이후**에 남아 있던 **125～129·131 도 완료**(앨범 머리색이 이야기 도중 바뀌던 문제 포함). **130 은 사람 몫**(거부 목록 파일).
그림이 대본을 부정하던 **132～138 도 완료** — 분기점과 여운 엔딩을 다시 그렸다(웃으며 손 흔들던 이별 장면, 간격이 없던 '좁혀지지 않는 간격', 밤으로 떨어진 노을 두 장).
**SCENE-009 도 이제 정해졌다** — 등록돼 있던 후보(`cf_ed1729_2`)가 목적·감정·연출 메모를 전부 만족한다.
죽은 카메라 데이터·쌓이던 파생물·창 기준으로 놓인 화면 **140～148 완료**(148 `git push` 는 2026-09-16 사용자 허락으로 밀렸다).
검사기 PASS · 자가진단 전체 통과 · 비밀 스캔 0건.
그록(xAI)을 공급자·브랜딩에서 내리고 감상까지 잇는 **149～153 도 완료** — 로컬 LLM 이 꺼져 있는
지금도 장면 작업이 끊기지 않는 것을 테스트가 지킨다(브리프 조립에는 모델이 필요 없다).
되돌릴 수 없는 다섯 자리 **154～158 도 완료** — 중복 백업 112.9MB, 클라우드로 나갈 뻔한 사적 대화,
잘리는 앱 아이콘과 없던 파비콘, 104DPI 짜리 8×10 마스터, 그리고 1화의 반복 구도(카메라 값 둘이
그림과 달랐고 그 거짓이 다양성처럼 보이게 하고 있었다 — 세 컷을 다시 구웠다).

---

## 1단계 · 보안·프라이버시 (가장 먼저)
- [x] **1** 개인 대화 로그(chatlog·talk_*)를 git 추적에서 제외
- [x] **2** `_require_scene` scene_id 정규식 검증 (경로 탈출 차단)
- [x] **3** LAN 모드 PIN 인증 (기동 시 콘솔 표시 · `logs/lan_pin.txt` 백업)
- [x] **4** `tools/secret_scan.py` — MakeFun 등 토큰 패턴 스캔 (A8 보완)
- [x] **5** 업로드 이미지 매직바이트 검증
- [x] **6** 다운로드 결과 매직바이트 검증 *(코드만 — 검증은 모의 서버로)*
- [x] **7** 공개 호스팅 프라이버시 경고 문서화
- [x] **8** 토큰 영구 등록(setx)·환경변수 관리 문서화

## 2단계 · 파이프라인 견고성 *(전부 모의 서버로 테스트 가능)*
- [x] **9** 지수 백오프 재시도
- [x] **10** task id를 장면 assets에 기록 (다운로드 실패 시 재수령)
- [x] **11** 부분 성공 반환 (일부 실패해도 저장분 보고)
- [x] **12** 생성 메타데이터 JSON 기록
- [x] **13** `--all-pending` 일괄 생성 배치 모드
- [x] **14** 장면별 in-flight 중복 생성 가드
- [x] **15** negative 문구를 프롬프트에 삽입 — ⚠️ **API에 negative 필드 없음 확인됨**, 프롬프트 텍스트에 녹이는 방식으로
- [x] **16** `--check` 토큰·크레딧 사전 점검 *(코드만)*
- [x] **17** 생성 이력 대장 (비용 추적 로그)
- [x] **18** CLI 폴링 진행 표시
- [x] **19** 생성 해상도 인화 기준 상향 *(설정·코드만)*
- [x] **20** 백그라운드화 + `/api/gen-status`
- [x] **44** compose count 상한(50) 검증
- [x] **45** `scenes_backup_*` → backups/ 이관 + 보존 상한

## 3단계 · 대화·뷰어 버그·UX (체감 큰 것)
- [x] **49** 대화 이력 서버 복원(이어하기)
- [x] **50** 전송 중 입력 잠금
- [x] **51** 타이핑 인디케이터
- [x] **52** 장기 기억(지난 대화 요약) 주입
- [x] **53** 현재 시각·요일 주입 (시간대 인사)
- [x] **54** 스토리 챗에 작품 컨텍스트 주입
- [x] **55** 대화·스토리 로그 원자 쓰기 + 크기 회전
- [x] **56** [처음부터] 확인 단계
- [x] **57** 타자기 속도 뷰어 설정 연동
- [x] **58** "이 순간을 사진으로" 버튼 — *프롬프트 생성까지만, 실생성은 허가 후*
- [x] **65** 뷰어 종료 시 [이어보기] 갱신 버그
- [x] **66** 뒤로가기 히스토리 스택화 (분기 경로)
- [x] **67** 선택지 호감도 변화(+♥) 연출
- [x] **68** 엔딩 카드 연출
- [x] **69** 재생 진행률 바
- [x] **70** 장면 이동 패널 썸네일+읽음 표시
- [x] **72** 갤러리 라이트박스
- [x] **82** 세로 스크롤 위치 저장

## 4단계 · 감상본·모바일 (기존 이미지 재활용 — 새 생성 불필요)
- [x] **59** 감상본에 분기·호감도·엔딩 재생 구현
- [x] **60** 감상본 백로그 패널
- [x] **61** 감상본 표지 CG 커버 — *승인된 기존 컷 사용*
- [x] **62** 한글 폰트 임베드 옵션
- [x] **63** 내보내기 옵션 UI 노출
- [x] **64** output/ 다운로드 라우트 (폰에서 감상본 받기)
- [x] **83** `/img` Cache-Control·ETag
- [x] **84** `/img` 썸네일 파라미터(?w=)
- [x] **85** PWA 캐시 버전 sha256 결정화
- [x] **86** PWA 내보내기 버튼
- [x] **87** PWA 아이콘 대표 컷 옵션 — *기존 승인 컷 사용*
- [x] **88** safe-area 대응
- [x] **89** 자동 재생 Wake Lock
- [x] **90** LAN 접속 QR 코드

## 5단계 · 스토리·데이터 정합성 (텍스트 작업)
- [x] **23** CHAR-002 '나' 앵커 구체화
- [x] **24** 프로필 성격·말투 필드 + persona 반영
- [x] **25** 캐릭터 바이블 작성·대화 주입
- [x] **26** 의상 배리에이션 앵커 세트 — *앵커 텍스트 + **장면 연결까지 완료**(장면 `wardrobe` → `prompt_build.wardrobe_variant`, 백로그 135). 옷을 바꾸는 것은 앵커가 아니라 배리에이션 `tags` 줄이었다 → docs/SCHEMA.md §1.6*
- [x] **29** SCENE-007 연출 필드 보강
- [x] **30** SCENE-007 프롬프트 시간대 모순 정리 — *텍스트 수정만, 리테이크는 보류*
- [x] **31** SCENE-003 characters에 CHAR-002 추가
- [x] **32** LOC-003 앵커에서 시간대 분리
- [x] **33** storyline.md에 7·8화 반영
- [x] **35** 여름 바다 에피소드 스토리라인
- [x] **36** 에피소드(화) 구분 필드 + 뷰어 화 선택
- [x] **37** 갈등·오해 비트 설계
- [x] **39** 분기(호감도) 미니 에피소드 — *장면 데이터까지*
- [x] **40** 속마음 괄호 규약 명문화
- [x] **41** manifest visual_style 정의 + 연동
- [x] **42** compose 지시문 연출 규칙
- [x] **43** compose 지시문 choices/branch 옵션
- [x] **46** scene_lint 등장 목록 밖 앵커 경고
- [x] **47** scene_lint time-프롬프트 불일치 경고
- [x] **48** scene_lint 카메라 어휘 표준화

## 6단계 · 인화·운영·문서
- [x] **71** ★ 즐겨찾기 인화 연동 (서버 저장 + 필터)
- [x] **73** sRGB ICC 프로파일 임베드
- [x] **74** fit(여백) 모드
- [x] **75** 크롭 미리보기 + 앵커 선택
- [x] **76** mm·포토카드(55×85) 프리셋
- [x] **77** 블리드 재단선 옵션
- [x] **78** 마스터 파일명 scene_order 접두사
- [x] **79** print_preflight TIFF 판독
- [x] **80** 인화 주문 실무 가이드
- [x] **81** 업스케일 — ⚠️ **MakeFun `/userUpscale` 은 유료**. 로컬 무료 경로(`--upscale step`·외부 실행파일 `auto`)로 구현
- [x] **91** `start_studio.ps1` 통합 기동
- [x] **92** webapp 회전 파일 로그
- [x] **93** `tools/doctor.py` 환경 점검
- [x] **94** backup restore·prune·--dest·--with-images
- [x] **95** 자동 스냅샷 스케줄러
- [x] **96** README 현행화
- [x] **97** CLAUDE.md 토큰 규약·엔진 반영
- [x] **98** templates/manifest.json 현행화
- [x] **99** 폰 튜토리얼 + 재해 복구 런북
- [x] **100** .gitignore 정리 + 회귀 테스트 추가

## 7단계 · 렌더 품질 (1차 렌더 이후 · ComfyUI 로컬 렌더라 토큰 0)
- [x] **101** 프롬프트 인원수·구도 태그 — `prompt_build.composition_tags` 가 앵커 **앞에**
  `1girl, 1boy, 2people, couple, …` 을 넣는다(엔진 중립 · docs/SCHEMA.md §2.3)
- [x] **102** `scene_lint` 역방향 경고 — `composition-cue` · `two-shot-single` (자문, PASS/FAIL 아님)
- [x] **115** 인물 태그 — `characters[].prompt_tags` 를 `prompt_build.character_tags` 가
  **그 인물 앵커 바로 앞**에 붙인다(앵커 원문 보존 → A6 그대로 · 엔진 중립 · docs/SCHEMA.md §2.3 · 회귀 U18).
  살아 있는 ComfyUI 로 51장을 구워 고른 결과다 — SCENE-001~007 재렌더로 흰 셔츠·파스텔 가디건이 돌아왔다

## 8단계 · 인화 파이프라인 (Pillow 필요 · 토큰 0)
- [x] **103** 인화 해상도 조치의 단일 출처 — `comfyui_client/makefun_client.size_recipe` 가
  바꿀 키와 값을 돌려주고 `print_preflight`·`print_export` 는 옮겨만 적는다
  (8의 배수 절삭 · ComfyUI hires 2배 상한까지 반영)
- [x] **104** `doctor` — 크기 조치를 클라이언트 문구 그대로 전달 + `인화 가능 규격` 행 신설
- [x] **105** `print_export` — 크롭이 **어느 변에서** 잘렸는지 안내 · 저장소 밖 출력 경로 허용
- [x] **106** 인화 회귀 테스트 PR03~PR06 (PR03·PR04·PR05 는 Pillow 없이도 실행된다)

---

## 9단계 · 뷰어 런타임 (실제 브라우저로 몰아본 결과 · 토큰 0)
- [x] **107** 선택지 손짓 가드 — 선택지를 **띄운 그 손짓**이 선택까지 하지 않는다
  (폰 탭 하나·데스크톱 더블클릭으로 분기가 정해지던 자리 · `PICK_GUARD_MS`)
- [x] **108** 호감도 장부 — 한 장면에서는 **마지막으로 고른 값 하나만** 셈에 넣는다
  (`affPicks`, 이어보기에도 저장·복원)
- [x] **109** 컷 토큰(`curImg`) — 늦게 도착한 `onload`·`onerror` 와 정리 타이머를 한 곳에서 심판
  (빠른 장면 전환·스킵에서 무대가 통째로 비던 문제)
- [x] **110** 손상된 저장 위치 복구(`intIn`) + 갈 곳 없는 `goto`·분기의 선형 폴백
  (가짜 엔딩 카드 · 아무 키도 듣지 않는 뷰어)
- [x] **111** 내보내기 도구의 보고 정정 — `--cover`·`--icon-from-cut` 이 조용히 폴백하고
  성공했다고 알리던 자리
- [x] **112** 뷰어 회귀 테스트 J06·J07 — DOM 대역으로 `vn_runtime.js` 를 **실제로 굴린다**

## 10단계 · 상태 기계·전송 경계 (토큰 0)
- [x] **113** 후보 등록은 `IMAGE` 까지 — `REVIEW_HUMAN` 승격은 `select_image` 하나만 찍는다
  (렌더 직후~사람이 고르기 전 구간에서 검사기 A3 가 FAIL 하던 자리 · 회귀 P19·P20)
- [x] **114** MakeFun `_once`/`_put_once`/`_fetch_bytes` — HTTP 가 아닌 응답은 두드린 주소를
  담은 **재시도 없는** `VNError`(이중 과금 차단 · 안내문에 토큰·서명 없음 · 회귀 M14)


## 11단계 · 프롬프트 레버·화면의 진실 (실측 135장 + 실제 폰 · 토큰 0)
- [x] **116** 넓은 컷의 거리 태그 — `prompt_build.SHOT_DISTANCE` 가 샷 이름 **바로 뒤에**
  `full body`(`extreme-wide` 는 `full body, from a distance`)를 한 번 붙인다.
  프로덕션 0/3 → 3/3. 좁은 컷·중간 컷은 예전 그대로다(회귀 U17 · 문서 동기 L06)
- [x] **117** 머리색 가드 — `image_generator.comfyui.negative_prompt` 에
  `blonde hair, platinum hair` 두 낱말(코드 기본 문구를 그대로 옮겨 적은 뒤에 붙였다 —
  매니페스트 값은 기본 문구를 **대체**한다). `prompt_tags` 는 손대지 않았다(긍정 쪽은 전부 실패).
  docs/SCHEMA.md §2.3 의 반대 서술도 실측에 맞춰 개정
- [x] **118** `gen_jobs.release(error=)` → `status().error` — 실패는 문구가 아니라 키로 말한다
  (토큰 없는 업스케일이 "저장했습니다" 로 끝나던 자리 · 회귀 U08)
- [x] **119** 서버가 거절할 행동은 권하지 않는다 — 승인 도장은 `REVIEW_HUMAN` 에서만,
  승인된 컷의 후보 썸네일은 잠그고 이유를 적고, 유료 업스케일은 토큰이 없으면 눌리지 않고,
  프롬프트 없는 장면에는 복사 버튼을 내밀지 않는다. APPROVED 재스캔의 사유도 화면에 남는다
- [x] **120** 결과 한 줄의 수명(`say`) — 성공 문구가 카드 재생성보다 오래 산다
- [x] **121** 머리말 칩 탐침(`probeEngines`) — 켜져 있는지 실제로 물어보고,
  모르는 동안에는 초록이라고 단정하지 않는다
- [x] **122** 숨은 탭은 그리지 않는다(들어올 때 갚는다) + 대화 CG·백로그·라이트박스 `?w=` 축소본
  — 숨은 `refresh()` 86,553,865 B → **0 B**(실측)
- [x] **123** 폰 레이아웃·낭독 구조 — `<select>` 138×46, 탭 영역 44px, 필터 막대 고정,
  수동 구성·그록 수동 접기(장면 탭 8,112→6,926px · 첫 카드 y 1,215→764 ·
  SCENE_PLAN 카드 1,352→307px), 절 제목 `<h2>` + `section aria-labelledby`
- [x] **124** 회귀 J08·J09 (DOM 대역으로 스튜디오 함수를 실제로 굴린다) · U08 확장

## 작품 완결 — 장면 12개 전부 APPROVED
- [x] **34** SCENE-008(귀갓길) — 인물 동일성이 깨진 후보를 버리고 프롬프트부터 다시 만들어
  재렌더(무료 ComfyUI). 고른 컷은 뒷모습 전신 투샷(`cf_fcb5b8_2`)
- [x] SCENE-011(호감 엔딩) · SCENE-012(여운 엔딩) — 두 갈래의 끝에 그림이 들어갔다.
  동작문 한 줄씩만 손으로 쓰고 대사·분기·`ending_label` 은 건드리지 않았다
  (`cf_d128f1_2` · `cf_ffa74b_1`)

## 완성 이후 — 앨범 일관성과 첫날의 사용자 (125–130)
- [x] **125** 저장소 전용 `.venv`(Pillow) + `start_studio.ps1` 이 먼저 찾기 + doctor 안내.
  실측: 장면 탭 `/img` 합계 **47.8MB → 1.2MB**(후보 60장, 전부 PNG → 전부 JPEG 썸네일).
  재렌더로 후보가 84장이 된 뒤에도 1.69MB — 장수가 40% 늘고도 28배 작다
- [x] **126** `gen_jobs` — pid 겹(즉시·확실) + 시각 겹(보수적)으로 잠금 회수를 두 겹으로.
  `VN_GEN_LOCK_DIR` 로 샌드박스 잠금을 명시 격리. 회귀 U11b·U11c 추가, U11 은 새 계약으로 갱신.
  돌연변이로 확인: pid 겹을 끄면 U11·U11c 가, in-process 지정을 빼면 U11b 가 실패한다.
  ⚠️ **A3 의 "샌드박스가 살아 있는 저장소에 잠금을 떨어뜨린다" 는 재현되지 않았다** — 실측 결과
  이미 샌드박스 트리를 보고 있었다(다만 그건 계약이 아니라 sys.path 교체의 부수효과였다)
- [x] **127** `snapshot --with-images` 실행(112개 파일 · 112.9MB) + `verify` 통과.
  경고 기준을 `list`·doctor 모두 **최신 스냅샷**으로 옮기고, 최신에 이미지가 없으면
  **이미지가 든 가장 최근 스냅샷의 restore 명령**까지 찍게 했다. 스케줄러는 붙이지 않았다
- [x] **128** 아홉 컷 재렌더(009·002·004·010·001·006·005·007·003, 보고서의 순서대로).
  승인본의 **그 시드**로 다시 굽고 후보 2장 이상을 눈으로 본 뒤 골랐다 — 되돌린 컷은 없다.
  앨범 hairV/skinV **0.181\~1.035(폭 0.854) → 0.174\~0.376(폭 0.202)**, 두 무리 사이의
  빈틈 **0.368 → 0.053**. 010 의 프레이밍도 고쳐졌다(머리 330px → 115px, 다른 와이드는 82/~90/113).
  감상본 재출력 완료
- [x] **129** 새 clone 온보딩 — `-LlmRoot`/`LOCAL_LLM_HOME`, `COMFYUI_HOME`·`LOCAL_LLM_HOME` 문서화,
  README 에 "방금 clone 했다면" 절, RECOVERY_RUNBOOK 의 `# RESULT: PASS` 정정,
  doctor·print_preflight 의 '파일 없음' 처방, studio.js 의 정직한 빈 칸.
  실측(이미지 없는 사본): 깨진 img **52 → 0**, 안내 문구 84개 · 갤러리 12칸 전부 표시
- [x] **131** `release()` 순서를 뒤집어(파일 → 소유) 창을 없애고, `status()` 에 '내 pid 면 다른 곳이 아니다' 안전판 추가. 회귀 U11d.
  게이트 재실행에서 CF07 이 실제로 깨져 찾은 결함이다 — 고친 뒤 시스템 python 전체 실행 **3회 연속 151/0/2-SKIP**, `.venv`·pilvenv 각 153/0/0
- [ ] **130** `check_protocol` A3 처방 한 줄 — ✋ **사용자 결정/적용 대기**.
  `tools/check_protocol.py` 는 에이전트 거부 목록이라 손대지 않았다. 넣을 문구는 커밋 `ee839a4` 본문에 있다


## 그림이 대본을 부정하던 자리 (132–138)
- [x] **132** `TIME_EN["노을"]` = `sunset, golden hour` + SCHEMA §2.3 실측 절 + 회귀 U21.
  승인 12컷 재측정에서 노을 컷 **두 장**(010 · 012)이 블루아워였다(012 하늘 온기 −33.7 =
  밤 컷보다 차갑다). 재렌더 결과 010 −3.4 → **+46.9**, 012 −33.7 → **+21.6**(둘 다 승인
  노을 대역 +23.8\~+50.6 안). 앞으로 옮기기는 해로웠고(하늘 파랑 .255 → .309) 화려한 문구는 과보정
- [x] **133** `comfyui.negative_prompt` 에 별 프린트 다섯 낱말 + SCHEMA §2.3.
  **프롬프트로는 못 고친다**를 실측으로 확정(태그 제거 0/2 · 묶는 말 0/2 · 네거티브 3/3).
  진짜 처방(regional prompting · 인물별 LoRA)은 코드 미구현 — 이 항목은 **증상 억제까지**다
- [x] **134** `is_distance_beat` + 장면 선택 필드 `intimacy`(distant/close) + 회귀 U17 확장·U20.
  12컷 실제 감정으로 재면 정확히 009 · 010 · 012 만 걸린다. 010 · 012 재렌더에서
  간격이 **실제로 생겼다**(009 는 되돌렸다 — 아래 재렌더 절)
- [x] **135** 의상 배리에이션 연결 — 장면 선택 필드 `wardrobe` + 배리에이션 `tags` +
  `wardrobe_variant`/`scene_wardrobe` + scene_lint 자문 4종 + 회귀 U18 확장·U19·U09 확장.
  **26 의 나머지 절반이 이것으로 닫혔다.** 기존 12장은 선언이 없어 프롬프트가 글자까지 동일하다
  (010 · 012 만 `default` 를 선언해 바닥 의상을 고정했다)
- [x] **136** `release` 순서를 파일 → 메모리로 뒤집고 `_acquire_file` 에 '내가 놓다 만 잠금' 안전판.
  회귀 U11d 확장(파일이 사라지는 순간의 `_JOBS`·`_OWNED` 상태를 직접 본다 — 예전 순서로
  되돌리면 실제로 FAIL 한다). 부하 검증: 전체 자가진단 **두 벌을 동시에** 돌려 157/0/0 · 155/0/2-SKIP
- [x] **137** `image_gen._fallback_engine` — ComfyUI 블록이 있으면 폴백도 무료 쪽으로.
  CF06 의 기존 단언 넷은 그대로 통과한다(옛 매니페스트에는 그 블록이 없다)
- [x] **138** 엔딩 카드는 **이번 경로에서 본 장면**만 세고, 세로 스크롤 리딩은 갈림길과
  두 결말에 이름표를 단다. 회귀 J10(최소 DOM 대역에서 한 경로를 끝까지 걷고 문구를 읽는다)

## 재렌더 — 분기점과 여운 엔딩 (연속성 보고서의 MUST/SHOULD)
- [x] **SCENE-010**(분기점) `cf_f4ed8d_2` → `cf_46618f_1` — 한 걸음 떨어져 각자 앞을 보고 걷는다.
  하늘 온기 −3.4 → +46.9. 의상 고정(분홍 카디건 + 베이지 플리츠 스커트)
- [x] **SCENE-012**(여운 엔딩) `cf_ffa74b_1` → `cf_3049e7_2` — 웃으며 손 흔들던 그림이 사라졌다.
  하늘 온기 −33.7 → +21.6, 명도 141.9 → 155.8. 의상은 010 과 동일
- [x] **SCENE-009**(오해의 시작) `cf_825b42_2` → `cf_ed1729_2` — 같은 편에 붙어 앉아 있던 그림이
  사라졌다. 동작 문장을 마주 앉은 구도로 고쳐 4장을 더 구웠지만(2장 + 3화 의상 정합 후 2장)
  전부 두 번째 인물을 배경으로 밀거나 주인공을 복제했다 — 이 구도는 시드가 정한다(기본
  프롬프트에서도 3장 중 1장). 이긴 것은 이미 등록돼 있던 후보다: 가방을 메고 일어선 그녀 ·
  돌아앉아 얼굴이 안 보이는 그 · 식은 잔. 저장된 프롬프트는 이 컷의 레시피와 바이트 단위로 같다

---

## 죽은 데이터·쌓이는 파생물·창 기준의 화면 (140–148)
- [x] **140** 조립부를 이름 붙은 조각으로 — `SEGMENT_ORDER` · 순수 함수 `assemble` ·
  부수효과 하나(`action_for`). 두 나무 프롬프트 180개 **바이트 동일**로 행동 보존 증명.
  회귀 U23(순서 상수·조립 순수성·동작 문장을 줬을 때 LLM 을 안 부른다)
- [x] **141** `camera.angle` 만 배선(`ANGLE_EN`) — `eye-level`·`front` 은 빈 문자열(실측 0/6).
  `framing`·`focus` 는 **일부러 안 한다**(0/6 · 두 번째 인물 3/3 삭제) — 근거를 SCHEMA 에 적어
  다시 재지 않게 했다. 어휘 정본은 `vn_core` 로(린터 ↔ 조립부가 같은 목록을 본다). 회귀 U24
- [x] **142** SCENE-009 재선정 — 위 '재렌더' 절 참조(ComfyUI 로 4장 추가 · 무료)
- [x] **143** `scene_lint` 의 `prompt-drift` 자문 — 저장된 프롬프트가 지금 조립부와 갈라진 컷을
  말한다(종료 코드 0 · info). 남은 세 장(005·006·011)은 **둘 다 그대로 두기로** 했다.
  회귀 U25(어느 조각이 갈라졌는지 · 동작 문장은 비교하지 않는다 · 자문 계층 계약)
- [x] **144** 파생물 보관 규칙 — 인화 마스터 최근 3종 · 캐시 64MB · 썸네일 32MB.
  후보·백업은 도구가 안 지운다(doctor `[용량]` 절이 크기와 '누가 지워도 되는가'를 보여 준다).
  회귀 U26
- [x] **145** 대사창·툴바를 그림 폭에 맞춘다(`--vnr-picw`·`--vnr-gut`) + 좁아지면 `[⋯]` 로 접는다.
  전/후 스크린샷 6장(1280×900 · 1600×1000 · 400×860): `output/qa/d6/`. 회귀 J11
- [x] **146** SCENE-002 음료 불일치 — 글자만 고쳤다(재렌더 없음)
- [x] **147** 문서 세 곳의 거짓 — 복구 런북(코드는 백업 zip 에 없다 · `verify` 누락의 진짜 원인 ·
  Pillow 7.3배) · 인화 가이드(4×6 208DPI · 5×7 166DPI) · 프라이버시(기본 경로는 아무것도
  안 나간다 · 원격 `COMFYUI_URL` 과 백업 `--dest` 의 실제 노출)
- [x] **148** `git push` — **2026-09-16 완료.** 사용자가 그 자리에서 명시로 허락했고
  그때 밀린 뒤로는 커밋마다 이어서 밀고 있다. 저장소 규약(CLAUDE.md · 에이전트의 push 금지)은
  그대로다 — 허락은 이 저장소에 한번 한 것이지 규칙을 고친 것이 아니다.
  밀리지 않은 커밋 수는 여전히 **세어서 본다**: `git rev-list --count origin/main..HEAD`
  (숫자를 여기 박아 두면 커밋할 때마다 낡는다 — 이 줄 자체가 한 번 그랬다).
  이전에 여기 적혀 있던 "origin/main 은 2026-08-25 이고 ComfyUI 엔진 파일이 통째로 없다" 는
  이제 사실이 아니다.

## 공급자를 이름까지 내리고 감상까지 잇는다 (149–153)
- [x] **149** 그록(xAI) 은퇴 — 삭제 `tools/grok_api.py` · `tools/xai_client.py`,
  개명 `make_grok_input.py` → `scene_brief.py` · `templates/grok-prompt-brief.md` →
  `scene-brief.md` · `grok-prompts-ko.md` → `prompt-frames-ko.md` ·
  `/api/grok-input` → `/api/scene-brief` · `project/grok_inputs/` → `scene_briefs/`.
  **`prompt.grok_output` 은 그대로 둔다** — 검사기가 수정 금지 상태로 이 키를 이름으로 읽고(A6),
  바꾸면 승인된 12장면이 전부 FAIL 이다. 이유는 SCHEMA §2.3 의 레거시 상자에 적었다.
  A8·`secret_scan` 의 `xai-` 패턴도 유지(탐지기이지 연동이 아니다). 회귀 P08·P18·W13·L09
- [x] **150** 감상본이 떨어뜨린 컷을 말한다 — `export()` → `(경로, data)`,
  `skipped`·`warnings` 는 `META_KEYS` 로 묶어 payload 에서 뺀다. 웹은 결과 줄에서 말한다.
  CLI 도 한 줄 추가(`--all 로 포함`). 회귀 V06
- [x] **151** 감상 탭 머리말(표지·제목·N장면·M화·승인 N/M·마지막 감상본 시각) +
  [감상본 내보내기] → **[감상본 만들어 열기]**(만든 뒤 바로 연다) +
  승인 문구 "승인됨 · N/M컷" 옆의 `[▶ 지금 보기]`. 사용법 세 문단은 `<details>` 하나로.
  회귀 J08(승인→감상) · J09(숨은 탭에서 표지를 받지 않는다)
- [x] **152** LLM 이 꺼진 날의 화면 — 스토리·장면 탭의 자체 안내, 대화 실패를 인물의
  대사창에서 상태 줄로, 칩은 "로컬 LLM 꺼짐 · 직접 입력". 회귀 J12(켜짐/꺼짐/모름 3상태) ·
  W37(같은 라우트를 모의 LLM 켜짐과 죽은 포트 양쪽에서 지난다)
- [x] **153** `orchestrator.api.base_url` 재배선 — `local_llm.base_url()` 의 3순위.
  우선순위 표는 SCHEMA §1.2 · ENV_SETUP · doctor 가 같은 말을 한다

---

## 되돌릴 수 없는 것들 — 백업·사적 기록·인화비·승인된 컷 (154–158)
- [x] **154** 중복 스냅샷 생략(`_same_as_latest`: 체크섬 + zip 존재 + **담을 파일 전부 포함**) ·
  `--force` · `prune --keep-images`(기본 3, `--keep` 과 별도) · 최신/최신 이미지 스냅샷은 보존.
  정리 실행: 바이트 동일본 `075622`(112.9MB)와 내용이 전부 다른 곳에 있는 `071751`(85.7MB)
  삭제, `014234`(23KB · 13개 파일이 git 에도 다른 zip 에도 없다)는 보존.
  회귀 B07(생략·`--dest`·`--force`·가벼운→이미지 포함은 생략 금지) · B08(무거운 쪽 상한)
- [x] **155** 백업의 개인 대화 기록 제외 — `vn_core.PRIVATE_PATTERNS`/`is_private_rel` 정본,
  zip·매니페스트 양쪽에서 제외, `verify` 는 '대상 아님' 한 줄, `--include-private`(+`--dest` 면
  확인, 비대화형이면 사적 기록만 빼고 계속). 문서 셋(SCHEMA §3.1 · PRIVACY_HOSTING · 런북).
  회귀 B09(픽스처 넷) · L06 확장(SCHEMA·.gitignore·vn_core 세 곳 대조)
- [x] **156** PWA 아이콘 purpose 분리(컷=`any` · 여백본=`maskable`) + 서비스워커 캐시 목록을
  실제 파일에서 생성 + 되돌렸을 때 옛 아이콘 삭제, 감상본·PWA 양쪽에 `<link rel="icon">`.
  실제 브라우저로 확인(before `/favicon.ico` 404 → now 요청 없음 · 매니페스트 errors 0).
  회귀 V07 · V05 확장. BANNED_DOM 유지
- [x] **157** 인화 하한 `vn_core.PRINT_DPI_FLOOR`=150 — 새 등급 '인화불가(⛔)',
  `print_export` 는 굽기 거부(필요 픽셀 + ComfyUI `base_long_edge_px` 까지 안내,
  `--allow-lowres` 로만 통과, 전부 거부면 rc 1), 웹/스튜디오도 같은 사실을 표시.
  크기 판독은 헤더로 — 돈을 막는 관문이 Pillow 유무에 딸려 가지 않는다. 회귀 PR07
- [x] **158** 1화 반복 구도 — SCENE-001 `medium`→`wide` · SCENE-006 `close-up`→`two-shot`
  (도달 불가였던 값) · SCENE-004 `medium`→`close-up` + 손을 앞으로 부르는 동작 문장(손으로 씀).
  002·003·005 는 그대로. 경로는 revise IMAGE → 후보 → 현재 컷과 비교 → 채택 → approve

---

## ⛔ 남은 5건 — 토큰 또는 사용자 결정 대기

| # | 항목 | 왜 멈춰 있나 | 지금 할 수 있는 준비 |
|---|---|---|---|
| **21** | 지혜 캐릭터 시트(표정 4종) | **토큰 필수** — 생성 1회 = 과금 | 프롬프트 4종 작성해 대기 |
| **38** | 표지 타이틀 컷 | **토큰 필수** | 프롬프트 작성해 대기 |
| **22** | MakeFun input_images 레퍼런스 고정 | **21 의 시트가 있어야** 붙일 대상이 생긴다. 코드 미구현 | 21 이 나오면 착수 |
| **27** | 대화 감정 → 표정 CG 전환 | **21 의 표정 시트**가 전제. 코드 미구현 | 21 이 나오면 착수 |
| **28** | '나' 얼굴 노출 정책 확정 | ✋ **사용자 결정 사항** (현행: 뒷모습 위주) | 사용자가 정하면 앵커에 반영 |

**34 는 여기서 빠졌다** — 엔진이 무료 로컬 ComfyUI 로 바뀐 뒤 토큰 없이 끝났다(위 '작품 완결' 절).

**22·27 은 "코드만 먼저" 상태가 아니라 아직 손대지 않은 상태다.** 붙일 레퍼런스가 없어
지금 짜 봐야 검증할 수 없기 때문이다. 21 이 끝나는 시점에 함께 진행한다.

## ⚠️ 코드는 됐지만 실호출 검증만 남은 것 (10건)

9 · 10 · 11 · 13 · 14 · 16 · 19 · 20 · 22 · 81
→ **모의(mock) MakeFun 서버로 자가진단까지 무료로 끝냈다.** 실제 API 한 번 호출로 하는
최종 확인만 사용자 허가 후에 한다. (22 는 위 표대로 아직 코드 자체가 없다.)

## 통합 화면에서 확정됐으나 아직 안 고친 것 (159–166)
2026-09-16 적대적 훑기가 확정한 32건 중 같은 날 고친 9건을 뺀 나머지.
항목 정의와 근거는 [BACKLOG.md](BACKLOG.md) §U 에 있다.

- [ ] **159** 인물 대화 [처음부터]가 아카이브 없이 저장본을 덮는다 (data-loss)
- [ ] **160** 답을 기다리는 중 다른 대화를 열면 입력이 죽고 남의 오류가 거기 뜬다
- [ ] **161** 그림이 끝나면 보고 있던 탭을 빼앗아 장면 탭으로 끌고 간다
- [ ] **162** 내보내기·가져오기 결과가 2.5초 폴링에 덮여 사라진다(내려받기 링크까지)
- [ ] **163** 첫 장면 전에 작업이 사라지면 진행 막대가 영원히 멈춘다
- [ ] **164** 조립 실패 뒤 한 마디만 보내면 [받은 글자 보기] 가 사라진다
- [ ] **165** `{"scenes":[…]}` 모양이면 장면별 진행과 중간 멈춤이 죽는다
- [ ] **166** 스토리 챗이 클라이언트 목록만 모델에 넘겨, 이력 로드 실패 턴은 기억을 잃는다
