# 적대적 감사 보고서 — 2026-08-24

6개 렌즈(상태머신·검사기 무결성·웹보안·xAI연동·compose 동등성·견고성)로 병렬 발굴 후,
각 결함을 격리 샌드박스에서 **실제 재현**으로 검증. selftest(T1~T20)가 못 잡던 사각지대를 겨냥.

- 총 **21건 CONFIRMED**(실행 재현) + 발굴 단계 already-run 다수.
- 편집 가능한 도구의 결함은 **모두 수정 + 회귀 테스트로 잠금**(selftest 37건 전체 통과).
- `check_protocol.py` 결함 9종은 프로토콜상 에이전트 수정 금지(.claude/settings.json deny) → 아래 **제안**만 기록.

---

## A. 수정 완료 (편집 가능 파일) — selftest 회귀로 잠금

| # | 심각도 | 파일 | 결함 | 수정 | 회귀 |
|---|---|---|---|---|---|
| 1 | **critical** | xai_client.py | cross-host 302 리다이렉트 추종으로 `Authorization: Bearer <키>`가 외부 호스트로 유출 | 리다이렉트 비추종 opener(`_NoRedirect`) + `base_url` https 강제(로컬호스트만 평문 허용) | **T33** |
| 2 | high | xai_client.py | 200인데 `content:null` → `None.strip()` AttributeError; 비-JSON/비-UTF8 본문 → 크래시 | JSON/decode 별도 try→RuntimeError, `isinstance(content,str)` 가드 | **T34** |
| 3 | high | webapp.py | 없는 scene_id로 register/select → `die()`의 `sys.exit`(SystemExit)가 `except Exception` 우회 → 응답 없이 스레드 사망(RemoteDisconnected) | 엔드포인트 진입 `_require_scene()` 존재검증 → RuntimeError→400 | **T25** |
| 4 | high | webapp.py | 정상 해피패스(register→select)에서 `review.auto`가 FAIL로 오염 → 프로젝트 RED | select에서 `auto=PASS` 낙관 기록 후 검사로 확정(A7 교착 해소) | **T26** |
| 5 | high | vn_compose.py | Grok이 스키마 다른 배열 반환 시 `AttributeError/TypeError`로 CLI traceback | 원소/필드 `isinstance` 흡수 + `order` 안전 정수화 | (T14/T20 유지) |
| 6 | high | vn_compose.py | 장면 생성 비원자적 — 중간 실패 시 부분 파일 + --force 백업과 겹쳐 반쯤 구성 | **전부 메모리 구성 후 WRITE_LOCK 안에서 일괄 저장** | (T22/T20 유지) |
| 7 | high | advance_scene.py | `revise IMAGE`가 `selected_image`를 안 비워, 재선택 없이 approve하면 **오래된 이미지가 APPROVED로 재잠금** | revise 시 selected 무효화 + `do_approve`에 REVIEW_HUMAN 상태 게이트 | **T29** |
| 8 | high | advance_scene.py | `new abc` 등 비표준 scene_id → order 무결성 파괴(검사기·진행표 불일치) | `^SCENE-\d+$` 형식 검증 | **T28** |
| 9 | medium | webapp.py | APPROVED 장면의 선택 이미지 삭제 후 재스캔 → status=APPROVED·auto=FAIL·selected='' 불변식 위반 | register 진입 APPROVED 가드(재스캔 잠금) | **T27** |
| 10 | medium | advance_scene.py | `select`에 위첨자('²' 등, isdigit=True·int 불가) → ValueError traceback | `isdecimal()` + int try/except | **T31** |
| 11 | medium | advance_scene.py | add-images 중간 파일 누락 시 앞 파일만 복사된 고아 파일 잔존 | 복사 전 **전체 존재 선검증** | **T30** |
| 12 | medium | advance_scene.py | 씬 파일 최상위가 dict 아닌 JSON(배열) → status/new가 AttributeError 크래시 | `load()`에 dict 검증 → 안내 종료 | **T32** |
| 13 | medium | make_grok_input.py | 무관한 형제 SCENE-*.json 손상 시 직전장면 탐색이 JSONDecodeError 크래시 | 탐색 루프 try/except로 손상 파일 건너뜀 | (기존 T 유지) |
| 14 | medium | vn_compose.py | `--force` 백업 mkdir `exist_ok` 없음 + WRITE_LOCK 밖 → 동시/연속 compose FileExistsError | 유니크 백업명(카운터) + 백업을 WRITE_LOCK 안으로 | (T20 유지) |
| 15 | medium | vn_compose.py | 요청 count와 생성 수 불일치가 조용히 통과 | 결과에 `warning` 필드로 개수 불일치 명시 | (T14 유지) |

## B. 검사기 제안 (check_protocol.py — 에이전트 수정 금지, 사용자 승인 필요)

아래는 전부 **검사를 더 엄격/견고하게** 만드는 변경이라(현재의 일부 PASS→FAIL, 크래시→FAIL),
"채점표를 고쳐 PASS를 만드는" 금지 행위와 정반대다. deny를 잠시 해제하고 지시하시거나 직접 적용하세요.
적용 후 `SCORECARD.md` "개정 이력"에 기록하고 deny 복구를 권장합니다.

1. **[high] 판독 불가 이미지가 APPROVED로 통과** (line ~311): 0바이트/손상 PNG는 `image_size()`가 None →
   `A3 SKIP`이라 승인 통과. → **REVIEW_HUMAN/APPROVED 단계의 selected_image는 size=None을 SKIP이 아니라 FAIL로**.
2. **[high] 씬 파일 최상위가 dict 아님** (line ~184): `sc.get(...)` AttributeError 크래시. → load 직후 `isinstance(sc,dict)` 아니면 A1/A2 FAIL 후 continue.
3. **[medium] manifest 최상위가 dict 아님** (line ~141): `required - set(manifest)` TypeError. → `isinstance(manifest,dict)` 가드.
4. **[medium] min_long_edge_px 문자열** (line ~169): `int < str` TypeError로 검사 중단. → `int()` 강제, 실패 시 A1 FAIL.
5. **[medium] min_long_edge_px ≤ 0** (line ~313): 해상도 게이트 무력화. → 양의 정수 검증, 아니면 DEFAULT로 대체 + FAIL.
6. **[medium] grok_output이 dict** (line ~256): `.strip()` AttributeError. → `isinstance(out,str)` 아니면 A6 FAIL.
7. **[medium] dialogue 원소가 dict 아님** (line ~225): `line.get` AttributeError. → `isinstance(line,dict)` 가드.
8. **[medium] A6 앵커 우회** (line ~269/274): 실제 anchor 없이 리터럴 ID(CHAR-001/LOC-001)만 있어도 PASS. → id 폴백 제거(또는 WARN 분리)하여 prompt_anchor 원문 포함 강제.
9. **[medium] A8 키스캔 확장자 갭** (line ~345): `.html/.js/.csv/.toml`, 줄바꿈 분할 키 미검출. → text_exts 확장 + 공백 허용 정규화.

> 참고: `tools/studio.html`은 실제 프론트 파일인데 A8 스캔 대상 확장자가 아니라, 만약 키를 하드코딩하면 A8이 놓칩니다(9번). 현재 저장소엔 키가 없어 실제 유출은 없음.

### B-2. 3차 감사(2026-08-25)에서 추가된 제안 — 역시 **수정 금지 파일**이라 기록만 함

10. **[medium] `REVIEW_AUTO` 는 실제로 존재하지 않는 상태인데 열거값에만 남아 있다**
    (`check_protocol.py:28` `SCENE_STATES`). 이 값을 **쓰는 도구도, 이 값에서 다음 단계로
    올려 주는 도구도 없다** — 승격은 `IMAGE` 에서만 일어나고(`scene_ops.register_images` ·
    `select_image`), `approve` 는 `REVIEW_HUMAN` 만 받는다.
    결과: 사람이 문서를 믿고 `status: "REVIEW_AUTO"` 라고 적으면 **검사기는 PASS 를 주는데
    승격·승인이 전부 막힌다.** `advance_scene revise <ID> IMAGE` 로 되돌리는 것 말고는 길이 없고,
    그 과정에서 `selected_image` 가 비워진다.
    → 제안: `SCENE_STATES` 에서 `REVIEW_AUTO` 를 제거한다. 단 `state_at_least` 가
    `SCENE_STATES.index()` 로 단계를 비교하므로 **제거하면 인덱스가 한 칸씩 당겨진다** —
    `REVIEW_HUMAN`/`APPROVED` 의 상대 순서는 그대로라 판정은 바뀌지 않지만, 옛 데이터에
    `REVIEW_AUTO` 가 남아 있으면 그 장면은 A2 FAIL(유효하지 않은 status)이 된다.
    적용 전에 `grep -rl REVIEW_AUTO project/scenes/` 로 확인하고, 있으면 먼저 revise 로 정리한다.
    **문서 쪽은 이미 조치했다**: `docs/SCHEMA.md` §2.1 과 `README.md` 흐름도에서 이 상태를 빼고
    "열거값에만 남은 미사용 상태 — 쓰지 마라" 경고를 넣었다(2026-08-25).
11. **[low] `A1~A7` 표기가 실제 구현(A8 포함)과 어긋난다** — `protocol/SCORECARD.md:3`
    ("`[A1]~[A7]` 라벨로 출력")과 `tools/check_protocol.py:2` ("SCORECARD v2 의 A1~A7 을 구현한다").
    같은 SCORECARD 안에 **A8 행이 존재하고**(13행) 검사기도 A8 을 출력하므로, 표기만 옛 범위에
    멈춰 있다. → 제안: 두 곳을 `A1~A8` 로 고친다. 판정 로직 변경 없음(문구만).
    (README·SCHEMA 등 편집 가능한 문서는 이미 `A1~A8` 로 적고 있다.)

> 10·11 모두 CLAUDE.md "채점표·검사기 개정 절차"의 대상이다 — 에이전트는 제안만 하고,
> 적용은 사용자가 하거나 deny 를 임시 해제한 뒤 지시한다. 적용 후 SCORECARD "개정 이력" 기록 + deny 복구.

## C. 설계/문서 괴리 (낮음)

- 뷰어(studio.html)는 APPROVED가 아닌 초안 장면도 재생함(README상 미리보기는 의도됨). **최종 전달용 export/DELIVERY 생성 도구는 미구현** — 문서와 구현 괴리. 추후 `deliver` 명령 추가 권장.
  **(2026-08 해소: `tools/export_viewer.py` 단일 HTML 감상본 · `tools/export_pwa.py` 설치형 번들 ·
  `tools/print_export.py` 인화 마스터. 셋 다 기본은 APPROVED 장면만 담고, 초안까지 넣으려면
  명시적으로 `--all` 을 줘야 한다 — 미리보기와 전달물의 경계가 코드에 생겼다.)**

## D. 3차 감사(2026-08-25) 문서·템플릿 지적 — **해소 완료**

| 지적 | 조치 |
|---|---|
| `talk_store` 아카이브(`project/story/*.archive.jsonl`)가 `.gitignore` 패턴(`talk_*.json`·`chatlog.json`)에 안 걸려 **개인 대화가 `git add .` 한 번에 커밋될 수 있었다** | `.gitignore` 에 `project/story/*.archive.jsonl` 추가 + `docs/SCHEMA.md` §3 표·§3.1·`docs/PRIVACY_HOSTING.md` 두 목록·`README.md` 프라이버시 절에 반영 |
| `templates/scene.json` 의 `print.crop_anchor:"center"` 가 새 장면 전부에 박혀 `print_export --anchor top` 을 조용히 무력화 | `print` 블록 제거. SCHEMA §2.8 에 "장면 값이 명령행보다 세다" 경고, §4 에 "템플릿에 기본값처럼 보이는 값을 두지 않는다" 표 추가 |
| `templates/manifest.json` 의 `talk.relationship: ""` 가 기본값(`"다정한 여자친구"`)을 이겨 페르소나가 `[관계] 상대는 너의 .` 로 깨짐 | 값을 채움 + SCHEMA §1.5 에 "비울 거면 키를 삭제" 경고 |
| SCHEMA 정확도 6건 (`dialogue` 배열이 A2 필수 키 · 도구 전용 필드 7개(`prompt`·`version` 누락) · `props`/`visual_style` 은 스튜디오 편집 불가 · `locations[].version` 행 누락 · props `reference_images` 는 미사용 · 사용량 로그의 `capped`/`want_px`/`cap_px`/`refetch` 누락 · scene_id 형식은 검사기가 안 본다) | 각 절에 반영 |
| `protocol/STATUS.md` "현재 위치" 가 "첫 작품 투입 대기(데모 데이터)" 로 정지 | 실제 상태(12장면·1~3화·승인 7·분기 2)로 교체, 스냅샷 날짜와 단일 출처 명시 |

> 위 조치는 전부 문서·`.gitignore`·템플릿에서 이뤄졌다. **검사기(`check_protocol.py`)와
> `SCORECARD.md` 는 손대지 않았다** — 관련 제안은 B-2(10·11)에 기록만 해 두었다.

## E. 4차 감사(2026-08-25) 문서·템플릿·예제 지적 — **해소 완료**

| # | 심각도 | 지적 | 조치 |
|---|---|---|---|
| E1 | **high** | **문서대로 하면 승인이 파기된다.** SCHEMA §2.1 이 승인 잠금을 "예외 없음"으로 단정했는데(`APPROVED 장면은 편집 경로에서도 거부되고, 먼저 revise 로 되돌려야 한다`), 코드에는 **print 예외**가 있다(`scene_ops.update_fields` 의 `print_only`, 이름을 준 것이 `scene_ops.set_crop` = `/api/set-crop`). 문서만 읽은 사용자가 승인 컷의 크롭을 바꾸려고 `revise` 를 부르면 `status` 가 내려가고 `review` 가 `PENDING` 이 되고 **`selected_image` 가 지워진다** — 인화 설정 한 글자에 사람 승인 결과물이 통째로 날아간다 | SCHEMA §2.1 에 "승인 잠금의 유일한 예외 — `print`" 경고 블록 추가(예외의 정확한 조건 `print` **단독** 요청까지 명시). §2.8 과 `docs/PRINT_ORDER_GUIDE.md` §2 크롭 절에 **"승인 후에도 크롭은 바꿀 수 있다 — `revise` 하지 마라(선택본이 지워진다)"** 한 줄씩 |
| E2 | medium | **예제가 문서 스스로 금지한 안티패턴을 담고 있었다.** `examples/scenes/SCENE-001.json` 의 `print.crop_anchor:"center"` 는 SCHEMA §2.8·§4 가 이름까지 붙여 금지한 값인데, README 는 이 파일을 복사하라고 안내하고 `selftest` 의 `fresh_scene` 픽스처도 이 파일을 복사한다 — **테스트 장면 20여 개가 전부 그 값을 달고 태어났다**(장면 값이 `--anchor` 를 조용히 이긴다) | `print` 블록 삭제. SCHEMA §2.8 의 "템플릿에 두지 않는다" 문장에 `examples/scenes/SCENE-001.json` 을 함께 명시 |
| E3 | medium | **재생 엔진 전달 스키마가 SCHEMA 밖에 있었다.** `{title, scenes:[{id,order,purpose,img,lines,ep,choices,branch,ending,ending_label}], dating, episodes}` 를 손으로 조립하는 생산자가 **둘**(`export_viewer.build_data` · `studio.js vnScene`). §5 "스키마를 바꿀 때" 를 성실히 따라도 이 둘을 안 건드리면 **새 필드가 재생 화면에만 안 나오는** 경로가 열려 있었다 | SCHEMA **§2.9** 신설 — 전달 스키마 모양 + 정본(`build_data`)·상세(`vn_runtime.js` 머리 주석)·두 번째 생산자(`studio.js`) 표, "생산자가 둘이다" 경고. §5 체크리스트에 **"감상 화면에 보여야 하면 두 생산자를 함께 고친다 — 한쪽만 고치면 스튜디오와 소장본의 연출이 갈린다"** 를 4번 항목으로 추가 |
| E4 | low | `logs/gen_locks/<scene_id>.lock` 이 §3 파생 데이터 표에 없었다 — **생성 거부(`이미 생성 중입니다`)의 원인이 되는 유일한 미문서 파생물** | §3 표에 행 추가 + **§3.5** 신설(거부 문구·20분 시각 기반 회수·지워도 되지만 정말 생성 중이면 이중 과금이 열린다는 조건까지) |
| E5 | low | `templates/manifest.json` 의 `image_generator` 에 `max_long_edge_px` 가 **없어서**, 문서가 세 곳(README·SCHEMA §1.3·PRINT_ORDER_GUIDE §1)에서 경고하는 절삭 함정의 **고칠 자리가 새 작품 파일에 보이지 않았다**(JSON 은 주석을 못 단다) | `"max_long_edge_px": 2048` 명시(= 코드 기본값, 동작 변화 없음) + `note` 에 "인화하려면 `output.min_long_edge_px` 와 함께 올린다(4x6 1800 / 5x7 2250 / 8x10 3600, 하드 상한 4096)" 와 `--check` 확인법 |

> E5 의 부작용 하나를 남겨 둔다(문서 담당이 고칠 수 없는 파일). `makefun_client.size_plan` 은
> `cap_is_default` 를 **키의 부재**(`"max_long_edge_px" not in _cfg()`)로 판정한다. 템플릿이
> 값을 명시하면서 새 작품의 `cap_is_default` 가 `False` 가 되어, `makefun_client --check` 의
> "주의 상한이 기본값 2048px 입니다…" 와 `doctor.py` 의 "인화용으로 올릴 때 함께 올리세요"
> **사전 안내가 더는 뜨지 않는다**(실제로 깎였을 때의 ERR 경고는 `capped` 판정이라 그대로 뜬다).
> → 제안: 판정을 **값 기준**(`_cap_px() == SIZE_MAX_PX`)으로 바꾸면 값을 적어 둔 새 작품에서도
> 안내가 살아난다. `makefun_client.py:274` 한 줄이며 판정 로직은 바뀌지 않는다.

> B-2 의 **11번(`A1~A7` 표기 → `A1~A8`)은 이번 라운드에도 그대로 열려 있다** — `SCORECARD.md:3`
> 과 `check_protocol.py:2` 는 수정 금지 파일이라 재확인만 하고 제안을 중복 기록하지 않았다.
