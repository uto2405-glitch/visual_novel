# 제작 백로그 100 — 항목 정의

5개 렌즈 코드 스캔(125건) + 세션 컨텍스트를 큐레이션한 실행 목록.
**이 문서는 각 번호가 무엇이고 왜 필요한지만 고정한다.**

> ## 상태는 여기에 적지 않는다
> 진행/완료 상태의 **단일 출처는 [NO_TOKEN_TASKS.md](NO_TOKEN_TASKS.md)** 다.
> 두 문서가 각자 체크박스를 들고 있으면 반드시 어긋나므로, 이 문서에는 상태 열이 없다.
> "지금 뭐가 남았나"는 NO_TOKEN_TASKS.md 를 본다.
>
> 번호는 **재사용하지 않는다.** 항목이 무의미해지면 지우지 말고 근거 칸에 사유를 남긴다.
> 새 항목은 101부터 이어 붙인다.

## A. 보안·프라이버시 (1–8)
| # | 항목 | 근거 |
|---|---|---|
| 1 | 개인 대화 로그(chatlog·talk_*)를 git 추적에서 제외한다 | 여친 페르소나와의 대화가 GitHub 원격에 푸시되는 중 |
| 2 | _require_scene 에 scene_id 정규식 검증을 추가해 경로 탈출을 차단한다 | `../..` 형 값이 장면 밖 파일에 닿을 수 있음 |
| 3 | LAN 모드 PIN 인증(기동 시 콘솔 표시)을 추가한다 | 같은 와이파이 기기가 과금 생성·승인 API 호출 가능 |
| 4 | tools/secret_scan.py — MakeFun 등 확장 토큰 패턴 스캔(A8 보완) | A8 은 xai- 패턴만 탐지 |
| 5 | 업로드 이미지 매직바이트 검증 | 확장자만 검사 중 |
| 6 | MakeFun 다운로드 결과 매직바이트 검증 | CDN 오류 페이지가 .png 로 저장될 수 있음 |
| 7 | PWA/감상본 공개 호스팅 프라이버시 경고 문서화 | 개인 소장 사진의 공개 노출 위험 |
| 8 | 토큰 영구 등록(setx)·환경변수 관리 문서화 | 재부팅 시 조용한 실패 방지 |

## B. 이미지 생성 파이프라인 (9–20)
| # | 항목 | 근거 |
|---|---|---|
| 9 | MakeFun 호출에 지수 백오프 재시도 | 일시 오류 한 번에 과금 생성 실패 |
| 10 | task id 를 장면 assets 에 기록(다운로드 실패 시 재수령) | 과금된 결과 유실 방지 |
| 11 | 부분 성공 반환(n장 중 일부 실패 시 저장분 보고) | 실패 격리 |
| 12 | 생성 메타데이터(프롬프트·모델·task·시각) JSON 기록 | scene_id 추적 원칙 준수 |
| 13 | --all-pending 일괄 생성 배치 모드 | 장면당 반복 클릭 제거 |
| 14 | 장면별 in-flight 중복 생성 서버 가드 | 폰+PC 동시 사용 시 중복 과금 방지 |
| 15 | negative prompt(no text/watermark) 지원 확인·반영 | 이미지 내 글자 금지 원칙 |
| 16 | makefun --check 토큰·크레딧 사전 점검 | 작업 도중 401 발견 방지 |
| 17 | 생성 이력 대장(비용 추적 로그) | 종량제 사용량 기록 없음 |
| 18 | CLI 폴링 진행 표시 | 5분 무음 대기 |
| 19 | 생성 해상도 인화 기준 검토·상향(매니페스트 연동) | 1024px 는 300DPI 엽서 미달 |
| 20 | gen-image 백그라운드화 + /api/gen-status 진행 조회 | 폰 브라우저 타임아웃 |

## C. 캐릭터 일관성 (21–28)
| # | 항목 | 근거 |
|---|---|---|
| 21 | 지혜 캐릭터 시트(표정 4종) 생성 → reference_images 등록 | 두 캐릭터 모두 레퍼런스 빈 배열 |
| 22 | MakeFun input_images 레퍼런스 고정 생성 경로 | 컷 간 얼굴 일관성. **21 의 시트가 있어야 연결 가능** |
| 23 | CHAR-002 '나' 앵커 구체화 | 컷마다 얼굴이 바뀜 |
| 24 | 프로필 성격·말투 필드 + persona 반영 | 외모만으로 인격 생성 중 |
| 25 | 캐릭터 바이블(취향·기념일·기억) 작성·대화 주입 | 대사·채팅 기억 불일치 방지 |
| 26 | 의상 배리에이션 앵커 세트(사복/계절) | 상태(모습) 변화 요구. 장면 연결 규약은 docs/SCHEMA.md §1.6 |
| 27 | 대화 감정 → 표정 CG 전환 | 미연시 몰입. **21 의 표정 시트가 있어야 연결 가능** |
| 28 | '나' 얼굴 노출 정책 확정(현행: 뒷모습 위주) | 사용자 취향 결정 사항 |

## D. 스토리·콘텐츠 (29–40)
| # | 항목 | 근거 |
|---|---|---|
| 29 | SCENE-007 연출 필드(framing·placement) 보강 | 혼자만 데이터 공백 |
| 30 | SCENE-007 프롬프트 시간대(sunset/night) 모순 정리 | 텍스트-비주얼 불일치 |
| 31 | SCENE-003 characters 배열에 CHAR-002 추가 | 목적·프롬프트와 불일치 |
| 32 | LOC-003 앵커에서 시간대 묘사 분리 | 밤 장면에 sunset 강제 삽입됨 |
| 33 | storyline.md 에 7·8화 반영 | 페르소나가 최신 이야기를 모름 |
| 34 | SCENE-008(귀갓길) 이미지 생성·승인 | 계획만 있고 이미지 없음 |
| 35 | 여름 바다 에피소드 스토리라인 확장 | 신규 콘텐츠 파이프라인 공백 |
| 36 | 에피소드(화) 구분 필드 + 뷰어 화 선택 | 2번째 이야기 구분 필요 |
| 37 | 갈등·오해 비트 설계(감정 기복) | 7장 전부 긍정 감정 |
| 38 | 표지 타이틀 컷 생성 | 작품 도입 없음 |
| 39 | 분기(호감도) 미니 에피소드 1개 | 분기 엔진이 콘텐츠 0개 |
| 40 | 속마음 괄호 규약 명문화 | 표기 흔들림 방지 |

## E. 장면 구성·프롬프트 품질 (41–48)
| # | 항목 | 근거 |
|---|---|---|
| 41 | manifest visual_style 정의 + gen-prompt/compose 연동 | 화풍이 코드에 하드코딩 |
| 42 | compose 지시문에 연출 규칙(샷 반복 금지 등) | lint 경고의 사전 예방 |
| 43 | compose 지시문 choices/branch 형식 옵션 | 분기 요구 지시문 부재 |
| 44 | compose count 상한(50) 검증 | 통제 없는 호출 확대 방지 |
| 45 | scenes_backup_* 를 backups/ 로 이관 + 보존 상한 | project/ 오염, 백업 중복 |
| 46 | scene_lint: 등장 목록 밖 앵커 경고 | A6 역방향 사각지대 |
| 47 | scene_lint: time-프롬프트 시간대 불일치 경고 | SCENE-007형 재발 방지 |
| 48 | scene_lint: 카메라 어휘 표준화 경고 | 표기 혼재 |

## F. 미연시 대화 (49–58)
| # | 항목 | 근거 |
|---|---|---|
| 49 | 대화 이력 서버 복원(이어하기) | 새로고침마다 대화 소실 |
| 50 | 전송 중 입력 잠금 | 중복 전송 |
| 51 | 타이핑 인디케이터 애니메이션 | 수십 초 무반응처럼 보임 |
| 52 | 장기 기억(지난 대화 요약) 주입 | 16개 창 밖 맥락 소실 |
| 53 | 현재 시각·요일 주입(시간대 인사) | 밤에도 상황 인식 없음 |
| 54 | 스토리 챗에 작품 컨텍스트(스토리라인·캐릭터) 주입 | 맥락 없는 기획 대화 |
| 55 | 대화·스토리 로그 원자 쓰기 + 크기 회전 | 손상·무한 성장 |
| 56 | [처음부터] 확인 단계 | 오터치로 대화 전체 소실 |
| 57 | 타자기 속도 뷰어 설정 연동 | 설정 일관성 |
| 58 | 대화 → "이 순간을 사진으로" 장면 생성 버튼 | 대화와 제작의 연결 |

## G. 뷰어·감상본 (59–70)
| # | 항목 | 근거 |
|---|---|---|
| 59 | 감상본 HTML 에 분기·호감도·엔딩 재생 구현 | 데이터만 내장, 재생은 선형 |
| 60 | 감상본 백로그(기록) 패널 | 폰 소장본에서 지난 대사 못 봄 |
| 61 | 감상본 표지 CG 커버 | 첫 화면이 텍스트뿐 |
| 62 | 감상본 한글 폰트 임베드 옵션 | 기기별 조판 차이 |
| 63 | 감상본 내보내기 옵션(범위·화질) UI 노출 | 서버는 받는데 UI 가 없음 |
| 64 | output/ 다운로드 라우트(폰에서 감상본 받기) | LAN 에서 받을 방법 없음 |
| 65 | 뷰어 종료 시 [이어보기] 갱신 버그 수정 | 새로고침 전까지 안 보임 |
| 66 | 뒤로가기 히스토리 스택화(분기 경로 추적) | 분기 후 ← 가 엉뚱한 장면 |
| 67 | 선택지 호감도 변화(+♥) 연출 | 선택 결과 체감 없음 |
| 68 | 엔딩 카드 연출(처음부터/갤러리 버튼) | 여운 없이 닫힘 |
| 69 | 재생 진행률 바 | 위치 감각 부족 |
| 70 | 장면 이동 패널 썸네일+읽음 표시 | 텍스트 나열뿐 |

## H. 갤러리·인화 (71–82)
| # | 항목 | 근거 |
|---|---|---|
| 71 | ★ 즐겨찾기 인화 연동(서버 저장 + export 필터) | localStorage 뿐, 안내와 불일치 |
| 72 | 갤러리 라이트박스(CG 크게 보기) | 확대 감상 불가 |
| 73 | 인화 마스터 sRGB ICC 프로파일 임베드 | 인화소 색 해석 편차 |
| 74 | fit(여백) 모드 추가 | cover 크롭뿐 |
| 75 | 크롭 미리보기 + 앵커 선택 UI | 잘림을 눈으로 못 봄 |
| 76 | mm·포토카드(55×85) 프리셋 | 한국 인화 규격 |
| 77 | 블리드 재단선 옵션 | 자가 인쇄 재단 기준 없음 |
| 78 | 마스터 파일명 scene_order 접두사 | 업로드 정렬 순서 |
| 79 | print_preflight TIFF 판독 지원 | 후보 허용인데 판독 불가 |
| 80 | 인화 주문 실무 가이드 문서 | 첫 주문 시행착오 |
| 81 | 업스케일 경로(MakeFun Upscale/로컬) 연동 | LANCZOS 확대뿐 |
| 82 | 세로 스크롤 읽던 위치 저장 | 항상 맨 위부터 |

## I. 모바일·PWA (83–90)
| # | 항목 | 근거 |
|---|---|---|
| 83 | /img Cache-Control·ETag | 같은 이미지 반복 전송 |
| 84 | /img 썸네일 파라미터(?w=) | 썸네일에 원본 MB 전송 |
| 85 | PWA 캐시 버전 sha256 결정화 | 매 실행 전체 재캐시 |
| 86 | PWA 내보내기 버튼(뷰어 탭) | CLI 전용 |
| 87 | PWA 아이콘 대표 컷 옵션 | 홈 화면 정체성 |
| 88 | safe-area(viewport-fit) 대응 | 노치와 대사창 겹침 |
| 89 | 자동 재생 Wake Lock | 감상 중 화면 꺼짐 |
| 90 | LAN 접속 QR 코드 표시 | IP 손입력 불편 |

## J. 운영·백업·문서 (91–100)
| # | 항목 | 근거 |
|---|---|---|
| 91 | start_studio.ps1 — LLM+스튜디오 통합 기동 | 수동 2회 실행 |
| 92 | webapp 회전 파일 로그(오류·생성 실패·LAN IP) | 장애 추적 불가 |
| 93 | tools/doctor.py 환경 종합 점검 | 실행 환경 진단 공백 |
| 94 | backup restore·prune·--dest·--with-images | 복원 경로·이미지 사본 없음 |
| 95 | 자동 스냅샷 스케줄러 스크립트 | 수동 백업 의존 |
| 96 | README 현행화(로컬 LLM+MakeFun 기준) | 구엔진 설명과 불일치 |
| 97 | CLAUDE.md 토큰 규약·엔진 반영 | MakeFun 토큰 보호 미문서화 |
| 98 | templates/manifest.json 현행화 | 새 작품이 구엔진으로 시작됨 |
| 99 | 폰 사용 튜토리얼 + 재해 복구 런북 | 지식이 흩어져 있음 |
| 100 | .gitignore 정리(백업 잔재·scratch)+회귀 테스트 추가 | 이력·백업 부풀림 |

## K. 렌더 품질 (101–)
1차 렌더로 드러난 결함 — 규격(검사기)은 통과하는데 **그림이 틀린** 부류다.

| # | 항목 | 근거 |
|---|---|---|
| 101 | 프롬프트 인원수·구도 태그(`prompt_build.composition_tags`) | 2인 장면이 한 명만 그려짐 — 앵커는 사람 수를 세어 주지 않는다(SCENE-005 에 CHAR-002 부재) |
| 102 | `scene_lint`: 인원수 단서 없음 · two-shot 단독 앵커 경고 | 101 의 역방향 — A6 는 앵커 유무만 보므로 '둘 다 있는데 한 명만 그려진' 컷을 못 잡는다 |
| 115 | 인물 태그(`characters[].prompt_tags` → `prompt_build.character_tags`) | 인원수를 고친 뒤에도 **옷과 머리색이 옆 사람에게 샜다** — CHAR-002 의 `plain white casual shirt` 가 거의 매번 검은 재킷, 이지혜의 `pastel knit cardigan` 이 크림색. A6 는 앵커 '글자'만 보므로 통과한 채 다른 옷이 그려진다. 실측(같은 시드 3개 × 변형 17종, 512x768/10steps)에서 순서 바꾸기·복장 꼬리·`BREAK`·네거티브 보강은 시드 노이즈와 구분되지 않았고, **태그를 그 인물 앵커 바로 앞에 두는 것**만 반복해서 들어맞았다(6/15 → 13/15) |

## L. 인화 파이프라인 (103–)
실물 인화 절반은 Pillow 가 없는 PC 에서 자가진단이 SKIP 되어 한 번도 실행되지 않았다.

| # | 항목 | 근거 |
|---|---|---|
| 103 | 인화 해상도 조치의 단일 출처(`size_recipe`) | "더 크게 생성하세요" 가 **어느 값을 얼마로** 를 못 말했다 — 상한 8의 배수 절삭(2250→2248)과 ComfyUI hires 2배 상한(1248→2496)을 빼먹어 8×10 은 시키는 대로 고쳐도 안 나왔다 |
| 104 | doctor — 크기 조치를 클라이언트 문구로 전달 + '인화 가능 규격' 행 | doctor 가 조치를 따로 지어 내 제자리로 돌려보냈다(이미 3600 인 값을 "3600 이상으로"). 인화 판정 자체도 doctor 에 없었다 |
| 105 | print_export — 크롭 방향 안내 · 저장소 밖 출력 경로 | 크롭 총량만 찍어 머리가 잘린 걸 파일을 열어야 알았다. OUT 을 저장소 밖으로 두면 다 구운 뒤 ValueError |
| 106 | 인화 회귀 테스트 PR03~PR06 | PR02 가 Pillow 부재로 SKIP 되어 인화 절반에 커버리지가 0 이었다 — 새 검사 4건 중 3건은 Pillow 없이도 돈다 |

## M. 뷰어 런타임 (107–)
사용자가 실제로 보는 화면. 실제 브라우저(Edge)로 감상본과 스튜디오 뷰어를 몰아본 결과 —
검사기도 자가진단도 통과하는데 **재생이 틀린** 부류다.

| # | 항목 | 근거 |
|---|---|---|
| 107 | 선택지 손짓 가드(`PICK_GUARD_MS`) | 선택지를 **띄운 그 손짓**이 그대로 버튼을 눌렀다 — 폰에서는 탭 한 번, 즉 엄지 위치가 분기를 정했고 선택지는 보이지도 않았다. 데스크톱 더블클릭도 같았다 |
| 108 | 호감도 장부(`affPicks`) | [장면]·[기록]으로 되돌아가 같은 선택지를 다시 고르면 값이 **쌓였다**(+8 세 번 = +24). 어떤 시작값에서도 눈금을 채워 원하는 엔딩을 열 수 있었다 |
| 109 | 컷 토큰(`curImg`) | 장면이 0.54초 페이드보다 빨리 바뀌면 앞 컷의 정리 타이머가 새 컷을, 새 컷의 타이머가 앞 컷을 지워 **그림이 한 장도 없는 검은 무대**가 남았다(스킵은 0.045초마다 넘어간다). 지나온 컷의 늦은 `onerror` 도 지금 그림을 지웠다 |
| 110 | 저장 위치 정수 검증(`intIn`) · 갈 곳 없는 `goto`/분기의 선형 폴백 | `"3"`·2.7·null 이 범위 검사를 통과해 `vi+1` 이 `"31"` 이 되거나 장면이 `undefined` 가 됐다 — 대사창이 빈 채 아무 키도 듣지 않았다. 목적지가 없는 `goto` 하나, 만족되는 분기 0 은 엔딩 장면도 아닌 곳에 **가짜 엔딩 카드**를 띄웠다 |
| 111 | 내보내기 도구의 보고 정정 | `--cover` 가 쓸 수 없는 장면이면 조용히 첫 컷으로 바꿨고, `--icon-from-cut` 은 폴백해 놓고 "대표 컷 중앙 크롭으로 생성" 이라고 알렸다 |
| 112 | 뷰어 회귀 테스트 J06·J07 | 분기 재생은 이 저장소에서 **눈으로만** 확인되던 마지막 자리였다. 최소 DOM 대역으로 `vn_runtime.js` 원본을 올려 호감도·분기·저장 복구를 실제로 굴린다 |

## N. 상태 기계·전송 경계 (113–)
게이트가 **정상 작업 중간에** 빨개지던 자리와, 유료 경로에서 traceback 이 그대로 새던 자리.

| # | 항목 | 근거 |
|---|---|---|
| 113 | 후보 등록은 `IMAGE` 까지 — `REVIEW_HUMAN` 은 선택이 찍는다 | `register_images` 가 자동 검사 PASS 만으로 `REVIEW_HUMAN` 을 찍었는데 A3 는 그 단계부터 `selected_image` 를 요구한다 — 렌더가 끝난 순간부터 사람이 고를 때까지 저장소가 **자기 검사기에 FAIL** 했다(순환 2에서 두 번 재현). 정상 중간 단계가 빨간불이면 게이트가 신호이기를 그만둔다 |
| 114 | MakeFun 전송 경계 — HTTP 가 아닌 응답은 주소를 담은 **재시도 없는** `VNError` | 그 포트에 MakeFun 이 아닌 것이 앉아 있으면 `http.client.BadStatusLine` 이 나는데 `URLError` 도 `OSError` 도 아니라 CLI 로 traceback 이 샜다. 재시도하면 이미 과금됐을 수 있는 POST 를 다시 보낸다(생성 시작은 task 를 만드는 순간 과금) |

## O. 프롬프트 레버·화면의 진실 (116–)
살아 있는 ComfyUI 로 135장을 구워 **무엇이 실제로 그림을 바꾸는지** 측정한 결과와,
실제 폰(msedge 400px)으로 스튜디오를 몰아본 결과. 검사기도 자가진단도 통과하는데
**그림이 지시를 안 따르거나 화면이 사실과 다른 말을 하던** 부류다.

| # | 항목 | 근거 |
|---|---|---|
| 116 | 넓은 컷의 거리 태그(`prompt_build.SHOT_DISTANCE`) | `camera.shot: "wide"` 가 지켜지지 않았다 — 샷 이름은 60여 개 태그 중 한 토큰이라 체크포인트가 흘려 본다(실측 6장 중 1장, 프로덕션 832×1248/30steps 에서는 0/3 이 클로즈업·미디엄 투샷). 앞으로 옮기기·두 번 반복·`distant view`·`scenery focus` 는 소용없거나 해로웠고(반복은 두 번째 인물을, `scenery focus` 는 인물 자체를 지웠다), **샷 이름 바로 뒤에 `full body` 한 토큰**만 3/3 으로 들어맞았다. 자리도 중요하다 — 구도 힌트 뒤로 밀면 프레임은 넓어져도 짝이 사라졌다 |
| 117 | 머리색 가드 — `comfyui.negative_prompt` 두 낱말 | CHAR-001 의 `light brown hair` 가 프로덕션에서 샴페인/애시 블론드로 나왔다(SCENE-002 0/3 · SCENE-006 0/2). 긍정 쪽 손질은 전부 실패 — `brown hair` 추가는 머리 영역 평균 명도 0.01~0.03 이동(눈으로 구분 불가), `medium`·`chestnut` 은 '밝은' 을 잃고, `(not blonde)` 는 가장 밝게 나왔으며(CLIP 에 부정은 없다), `(tag:1.3)` 은 엔진 전용 문법이라 중립 규약 위반. 네거티브 `blonde hair, platinum hair` 만 0.06~0.29 이동으로 5/5. **SCHEMA §2.3 의 "네거티브는 쓰지 않는다(흰 액자 부작용)" 는 이 측정이 뒤집었다** — 2낱말 추가에서 흰 테두리는 재현되지 않았다(가장자리 흰 픽셀 수가 기준본과 동일) |
| 118 | 실패의 단일 신호 — `gen_jobs.status().error` | 실패가 사람용 문구("실패: …")에만 남고 화면이 보는 `error` 키는 영영 오지 않았다. 토큰 없는 업스케일이 "확대본을 저장했습니다" 로 끝났다 — 유료 경로에서 사용자가 한 번 더 눌러 두 번 결제하게 만드는 거짓말이다. ComfyUI 가 꺼져 있으면 카드가 아무 말 없이 비었다 |
| 119 | 서버가 거절할 행동을 권하지 않는다 | [승인 도장 찍기]가 APPROVED 카드 9장에도 붙어 있었고(서버는 `REVIEW_HUMAN` 에서만 승인), 승인된 컷의 후보 썸네일 47개가 전부 눌리는 버튼이었으며 거절 답이 폰에서 실행할 수 없는 셸 명령이었다. 유료 [⬆ 인화용 업스케일]은 토큰 없이 과금 확인창을 띄운 뒤 실패했고(보조 생성 버튼에는 이미 있던 가드), 프롬프트 없는 장면의 [프롬프트 복사]는 빈 문자열을 복사하고 "복사됨 ✓" 라고 했다. APPROVED 재스캔의 '하지 않은 이유'(`note`)는 버려졌다 |
| 120 | 결과 한 줄의 수명(`say`) | 성공 문구가 만들어진 지 0.2초 만에 카드 재생성으로 덮여 사라졌다(승인·폴더 스캔은 아무 일도 없는 것처럼 보였고, 화면 낭독은 "…중" 뒤 침묵). 유료 업스케일 한 줄만 이미 이 보호를 갖고 있었다 |
| 121 | 머리말 칩은 '지금 응답한다' 를 말한다 | 두 칩이 설정값만 보고 항상 초록이었다 — 로컬 LLM 이 꺼져 있는데도 초록, 죽은 주소를 가리켜도 초록. 두 줄 아래 대화 탭은 같은 화면에서 "로컬 LLM 꺼짐" 을 띄우고 있었다. 폰에서는 이 칩이 유일한 상태 표시다 |
| 122 | 숨은 탭 재렌더 차단 + 축소본 요청 | `refresh()` 한 번이 화면에 없는 장면·갤러리까지 다시 그려 숨은 썸네일 61장 **86,553,865 B** 를 내려받았다(카드 조작·[처음부터 감상]마다). 대화 배경 CG·백로그 사진·라이트박스는 `?w=` 없이 원본 PNG(장당 1.3MB)를 요청했다 |
| 123 | 폰 레이아웃·화면 낭독 구조 | `<select>` 7개가 16px 글자를 21px 상자에 담아 글자가 잘렸고(전역 `*{padding:0}` 이 UA 여백을 지웠다), 체크박스 13px·`<summary>` 19px 로 44px 미만 탭 영역이 27개였다. 장면 탭은 8,112px 인데 첫 카드가 y=1,215 에서 시작하고 필터 막대는 스크롤과 함께 사라졌다. 문서 전체의 제목이 `<h1>` 하나뿐이라 제목 이동이 갈 곳이 없었고 `<section>` 6개에 이름이 없었다 |
| 124 | 회귀 J08·J09 + U08 확장 | 위 결함들은 전부 "폰으로 눌러 보면 아는" 종류라 잠금장치가 없으면 조용히 되돌아간다. J08 은 DOM 대역으로 장면 카드 함수를 **실제로 굴려** 단계별 버튼·잠금·결과 문구 생존을 확인하고, J09 는 `refresh`/`selectTab` 을 굴려 숨은 탭 렌더 0 과 `?w=` 규칙을 확인한다. U08 은 실패가 `error` 키로 오는지 본다 |

## P. 완성 이후 — 앨범의 일관성과 첫날의 사용자 (125–)
작품이 12컷 전부 APPROVED 가 된 **뒤에** 남아 있던 것들. 검사기·자가진단·린터가 전부
초록인데도 사실인 결함들이라, 게이트가 볼 수 없는 자리를 사람이 재서 찾았다.

| # | 항목 | 근거 |
|---|---|---|
| 125 | 저장소 전용 `.venv` — 스튜디오가 썸네일을 만들 수 있게 | 스튜디오를 띄운 파이썬에 Pillow 가 없어 `webapp.make_thumb` 이 ImportError 를 None 으로 삼켰고, `/img` 라우트는 그 None 을 보고 **원본 PNG 를 그대로** 보냈다. 실측: 같은 파일에 `?w=224` 를 붙이나 안 붙이나 1,331,949 B 로 동일 — 장면 탭 한 번에 47.8MB(폰 18.1MB). 시스템 파이썬은 건드리지 않기로 했으므로 저장소 안 `.venv` 에만 Pillow 를 두고 `start_studio.ps1` 이 그쪽을 먼저 찾게 한다. `tools/*.py` 의 표준 라이브러리 전용 규칙은 그대로다 — `.venv` 는 **실행하는 인터프리터**의 성질이지 코드 의존성이 아니다 |
| 126 | `gen_jobs` — 죽은 잠금은 즉시 회수, 샌드박스 잠금은 샌드박스에 | 회수 규칙이 mtime 하나뿐이라 렌더를 죽이면 같은 장면이 `STALE_SEC`(20분) 잠겼다(CF07 을 한 번 깨뜨렸다). pid 겹을 앞에 두되 **이 기기의 죽은 pid 임이 확실할 때만** 연다 — 다른 기기·옛 형식 잠금·권한 거부는 전부 시각 규칙으로 넘긴다(중복 과금 보증 유지). Windows 의 `os.kill(pid, 0)` 은 조회가 아니라 TerminateProcess 라 **묻는 순간 죽인다** — 조회 전용 핸들만 연다. 잠금 폴더는 `VN_GEN_LOCK_DIR` 로 명시 지정한다(예전엔 sys.path 교체의 부수효과로 우연히 맞았다) |
| 127 | 이미지 포함 스냅샷 · 경고 기준을 '최신 스냅샷' 으로 | 완성된 12컷이 `images/raw` 한 곳에만 있었다(git 제외 + 유일 스냅샷은 체크섬만). 게다가 경고 조건이 `list` = "어느 스냅샷에도 없을 때", doctor = "최근 3개 중 하나라도 있으면 OK" 라, 이미지 든 스냅샷 하나만 만들면 그 뒤로 영원히 잠잠해진다 — `restore` 가 기본으로 겨냥하는 **최신** 복구 지점에 앨범이 없는데 초록불이 켜진다 |
| 128 | 앨범 머리색 일관성 — 아홉 컷 재렌더 | 머리색 네거티브가 작업 중간(05:51)에 들어와, 그 전에 그린 여덟 컷의 이지혜는 샴페인/플래티넘이고 008·011·012 만 갈색이었다. 실측 hairV/skinV 가 0.638\~1.035 와 0.181\~0.270 두 무리로 **끊겨** 있었다(그 사이 0.270→0.638 에는 컷이 하나도 없다). 009 는 프롬프트가 한 세대 뒤져 인물 태그가 없어 카디건이 크림·상대 셔츠가 베이지(S38.0), 010 은 `shot: "wide"` 인데 머리 330px 의 가슴 위 투샷이었다 |
| 129 | 새 clone 온보딩 — 자리표시자 경로 · 72건 FAIL · 화면의 침묵 | 갓 clone 한 사람이 **사람에게 묻지 않으면** 못 넘는 자리가 셋이었다. (1) `start_studio.ps1` 의 `c:\Users\USER\claude\local_llm` 은 글자 그대로의 자리표시자인데 파라미터도 환경변수도 설치 문서도 없었다 — 스토리·장면·프롬프트·대화 네 탭이 그 서버에 달려 있다. (2) 첫 명령이 `RESULT: FAIL — 실패 72건` 을 내는데 처방이 0줄(`grep '→'` = 0)이고, 그 상황을 위해 쓰인 RECOVERY_RUNBOOK 은 같은 줄에 `# RESULT: PASS` 라고 적어 두었다. (3) 스튜디오는 깨진 썸네일 52개와 빈 갤러리 12칸을 이유 없이 보여 줬다 — 서버는 `image_url:null` 로 알고 있었는데 화면만 몰랐다. `COMFYUI_HOME` 은 저장소 어느 문서에도 없었다 |
| 131 | `gen_jobs.release` 의 순서 — 파일을 먼저 지우고 소유 표시를 나중에 | `_OWNED` 에서 먼저 내리고 잠금 파일을 나중에 지우는 바람에, 그 사이 한 순간 `status()` 가 **자기 pid 가 적힌 잠금**을 보고 "다른 곳에서 생성 중입니다" 라고 답했다 — 스스로를 남이라 부른 셈이다. 렌더가 끝났는데 진행 표시가 멈추지 않아 화면은 완료 문구를 영영 못 받는다. 창이 좁아 CF07 이 단독으로는 통과하고 전체 실행에서만 이따금 깨졌다(A3 가 적어 둔 그 실패의 진짜 원인). 126 과는 다른 결함이다 — 그쪽은 '남의 잠금을 언제 여는가', 이쪽은 '내가 놓은 잠금을 내가 어떻게 읽는가' 다 |
| 130 | (제안) `check_protocol` A3 실패에 처방 한 줄 | 129 의 (2) 를 가장 정확히 고치는 자리는 검사기 자신이지만, 이 파일은 에이전트 거부 목록(`.claude/settings.json`)이라 손대지 않는다. 같은 안내는 README·doctor·RECOVERY_RUNBOOK 에 넣어 두었다. 사람이 적용할 때의 문구는 커밋 `ee839a4` 본문에 그대로 있다 |

## Q. 그림이 대본을 부정하던 자리 (132–)
완성된 12컷을 **이야기와 맞춰** 다시 읽고, 살아 있는 ComfyUI 로 128장을 구워
"무엇이 실제로 그림을 바꾸는가"를 잰 결과. 검사기·자가진단·린터가 모두 초록인데
**그림이 대본의 반대를 그리고 있던** 부류다(연속성 보고서 C1–C4).

| # | 항목 | 근거 |
|---|---|---|
| 132 | 노을은 한 낱말로 버티지 못한다 — `TIME_EN["노을"]` 에 `golden hour` | 시간대 낱말은 ~70토큰 프롬프트의 **맨 끝 한 토큰**이라 하늘이 넓게 보이는 컷에서 장소 앵커의 밤 조명 낱말(`low street lamps` · `city skyline across the water`)에 밀린다. 승인된 12컷을 재니 노을 컷 **두 장**(010 · 012)이 실제로 블루아워였다 — 012 의 하늘 온기 −33.7 로 **밤 컷(−10.1)보다 차갑다**. 3시드 × 7안: 같은 낱말을 앞으로 옮기면 오히려 나빠졌고(하늘 파랑 .255 → .309) 화려한 문구·반복·네거티브는 과보정(명도 182~214)이었다. `golden hour` 한 낱말만 3/3 을 고치고 승인 컷 색 대역 안에 들어왔다. 멀쩡한 노을 셋이 close-up·two-shot 인 것은 우연이 아니다 — 하늘을 안 보여 준다 |
| 133 | 인물별 속성 귀속은 **프롬프트로 못 고친다** — regional prompting 이 필요하다 | 이지혜의 앵커에 박힌 `small star earrings` 가 상대 인물의 셔츠에 프린트로 찍혔다(후보 84장 중 인물을 넘어간 별/프린트 4장 = 4.8%). 결정적 증거: 그중 둘은 `prompt_tags` 가 생기기 **전에** 나왔다 — 그 프롬프트에는 태그가 아예 없다. 알려진 재현 조건 3개에서 태그 제거 0/2 · `she wears …` 로 묶기 0/2 · 네거티브 3/3. 원인은 체크포인트다(SDXL/Illustrious 의 CLIP 조건화에는 인물 분리가 없다 — 같은 앨범에 **남자가 지혜의 분홍 가디건을 입은** 후보가 있다). 네거티브는 증상 억제이고 진짜 처방은 Attention Couple 류 regional prompting 이나 인물별 LoRA/레퍼런스다 |
| 134 | 감정이 '거리'인 컷에서는 `couple` 을 **뺀다**(`is_distance_beat`) | 태그가 비트를 안 보고 붙어, 오해가 시작되는 컷(009) · 한 걸음 떨어져 걷는 분기점(010) · 먼저 돌아서는 엔딩(012)이 전부 붙어 있는 연인으로 그려졌다 — 분기를 결정하는 컷에서 그림이 대본을 부정한다. 실측(010 · 3시드 × 5안): **지우는 것만** 통했다(3/3 간격 · 3/3 두 인물 유지). 거리 문구로 **교체**하면 인원수 태그 줄이 희석돼 두 번째 인물이 사라졌다(문구 2/3 · 문구+네거티브 3/3 손상). 판정은 `emotion` 만 본다 — `purpose` 를 훑으면 SCENE-011(호감 엔딩)의 목적 "**오해**가 풀리고…" 때문에 가장 따뜻한 컷에서 애정 태그가 빠진다 |
| 135 | 의상 배리에이션을 **연결한다**(26 의 나머지 절반) | 정의만 있고 아무도 읽지 않던 `wardrobe_variants[]` 에 장면의 `wardrobe` 를 붙였다. SCHEMA 가 오래 제안해 두었던 "앵커 안 의상 구절을 갈아 끼우기" 는 실측에서 **듣지 않았고**(가슴 분홍 비율 .0636 → .0744 = 변화 없음) 앵커 원문이 사라져 **A6 도 깬다**. 움직인 것은 태그 줄이었다(.0095, 4/4). 그래서 배리에이션에 선택 필드 `tags` 를 두고 앵커는 원문 그대로 남긴 뒤 뒤에 `wearing <anchor>` 를 덧붙인다 — 검사기 수정 0줄. 곁가지로 드러난 결함: 새 필드를 `EDITABLE_FIELDS` 에만 올리고 `_apply_field` 에 갈래를 안 만들면 편집 경로가 **"저장했다"고 답하면서 아무것도 안 쓴다** |
| 136 | `release` 의 순서 — 잠금 파일이 먼저, 메모리 표시가 나중 | 131 이 닫은 것은 `_OWNED` 쪽 창이고 이것은 `_JOBS` 쪽 창이다. 파일을 지우기 **전에** 메모리를 '끝남' 으로 바꾸면, 그 사이 `status()` 는 running=False 라 답하고(화면이 [생성] 을 다시 열어 준다) 바로 이어지는 `claim()` 은 남은 파일을 보고 **내 pid 를 가리키며** 거절한다 — 끝났다고 말해 놓고 누르면 바쁘다고 하는 화면. 기계가 바쁠수록 창이 넓어져 전체 자가진단에서 CF07 이 이따금 깨졌다(외부 검토: 8회 중 3회). 안전판으로 `_acquire_file` 도 '내 pid 인데 내 소유가 아닌' 잠금을 회수한다 — `status()` 만 그 규칙을 알면 두 층이 계속 다른 말을 한다 |
| 137 | 엔진 폴백의 방향 — 오타 한 글자가 **유료 호출**이 되지 않게 | `image_generator.engine` 을 못 읽으면 무조건 MakeFun(유료)으로 떨어졌다. `engien: "comfyui"` 같은 오타 하나가 과금 경로가 된다는 뜻이다. 매니페스트에 ComfyUI 블록이 있으면 무료 쪽으로 떨어지게 조건을 붙였다 — 옛 매니페스트에는 그 블록이 없으므로 "engine 키가 없던 시절 = MakeFun" 호환은 글자 그대로 유지된다 |
| 138 | 감상본이 사실이 아닌 말을 하던 두 자리 | ① 엔딩 카드가 **작품 전체 장면 수**를 "감상 완료" 라고 적었다 — 어느 분기를 걸어도 본 것은 11장이고, 여운 엔딩 쪽 독자는 못 본 장면까지 축하받는다. ② 세로 스크롤 리딩은 분기를 모르고 12장을 차례로 싣는다: 선택지 셋이 전부 일어난 일처럼 찍히고, 호감 엔딩의 "…없던 걸로 해줄게" 바로 다음 줄에 여운 엔딩의 "그냥 여기서 헤어지자" 가 온다. 표지에서 [처음부터 감상]과 거의 같은 무게로 권하는 입구다. 읽는 방식은 그대로 두고 **이름표**로 고친다(갈림길 · 엔딩 이름) |
| 139 | 새 필드를 **편집 경로에서만** 좁힌 것은 절반이다 — 손편집 JSON 에는 관문이 없다 | 132–138 이 만든 `intimacy` · `wardrobe` 는 `_apply_field` 가 오타를 거절하고 dict 를 강제한다. 그런데 장면은 디스크의 JSON 이고 이 저장소는 손으로도 고친다 — 그 경로에는 아무것도 없었다. 검사기는 두 필드를 보지 않으므로(A2/A6 에 여섯 가지 모양을 넣어도 전부 PASS) 잘못 적힌 낱말이 **아무 데서도 티가 나지 않았다.** `intimacy` 쪽은 단순한 침묵이 아니라 **역전**이다 — `is_distance_beat` 는 값이 비어 있지 않으면 `emotion` 을 아예 안 보고 `== "distant"` 만 묻는다. 실측: SCENE-009(서운함)에 `'distatn'` 을 적으면 판정이 True → False 로 뒤집혀 134 가 뺀 애정 태그가 도로 붙고, 린터는 "특이사항 없음" 이라고 답했다. `wardrobe` 가 dict 가 아니면 `scene_wardrobe` 가 조용히 `''` 를 돌려줘 옷이 영영 안 바뀐다. 자문 둘(`intimacy-value` · `wardrobe-shape`)로 메웠다 — 규격 위반이 아니라 설계 사고라 자문 계층이 맞는 자리이고 종료 코드는 그대로 0 이다 |

## R. 죽은 데이터·쌓이는 파생물·창을 기준으로 놓인 화면 (140–)
완성된 앨범을 놓고 **측정으로** 찾은 다음 묶음. 세 갈래다 — 스키마가 있다고 말하지만
파이프라인에 닿지 않던 데이터(140–141), 저장된 것과 지금 코드가 갈라진 것을 아무도
말해 주지 않던 자리(142–143), 그리고 다시 만들 수 있는 것을 무한히 쌓거나 그림이 아니라
창을 기준으로 놓여 있던 화면(144–145).

| # | 항목 | 근거 |
|---|---|---|
| 140 | 조립부를 **이름 붙은 조각**으로 — `SEGMENT_ORDER` · `assemble` · `action_for` | `compose_image_prompt` 한 몸에 네트워크 호출·문장 정규화·매니페스트 조회·아홉 번의 위치 append 가 섞여 있었다. 실측으로 산 순서(거리 태그는 샷 바로 뒤 · 인원수는 앵커 앞 · 시간대는 맨 끝)가 **'몇 번째 줄이냐'로만** 적혀 있어, 새 조각을 더하려면 맞는 줄을 찾아 끼워야 했다. 순수 함수 `assemble` 은 매니페스트도 LLM 도 안 보므로 순서를 장면 없이 되물을 수 있고, 부수효과는 `action_for` 하나에 모였다. 행동 보존은 두 나무에서 프롬프트 180개(12장면 × 변형 15종)를 뽑아 **180/180 바이트 동일**로 확인했다 |
| 141 | 카메라 네 필드 중 **하나만** 프롬프트에 닿게 한다(`ANGLE_EN`) | `camera.angle`·`framing`·`focus` 는 SCHEMA 가 "프롬프트 입력" 이라 적어 두었지만 어느 것도 이미지 모델에 닿지 않았다(닿던 것은 `shot` 하나). 78장을 구워 잰 결과: **angle 만 배선할 값이 있다.** 단 `eye-level`·`front` 은 빈 문자열이다 — 12장면이 전부 `eye-level` 이라 낱말을 실으면 뜻 없는 토큰 하나를 12장 전부에 더하면서 승인된 프롬프트를 전부 바꾸는데, 정작 변화량은 시드 노이즈의 0.23~0.61배이고 카메라가 움직인 컷은 0/6 이었다. 어휘 자체에는 힘이 있다(`from above` 6/6 · `from below` 4/6). framing 은 **이미 동작 문장이 그 구도를 말하고 있어** 0/6, focus 는 Danbooru 표기로 넣으면 샷을 덮어써 두 번째 인물 3/3 · 불꽃 2/3 을 지웠다 — 둘은 사람이 읽는 연출 메모로 남기고 그 근거를 SCHEMA 에 적어 다시 재지 않게 했다. 어휘 정본은 `vn_core` 로 내렸다(린터가 고치라는 값과 조립부가 알아듣는 값이 갈리면 각도가 경고 없이 사라진다) |
| 142 | SCENE-009 — 그림이 '오해의 시작' 을 부정하고 있었다 | 승인 컷은 목적(먼저 일어선다)·감정(서운함)·`camera.framing`(테이블을 사이에 둔 두 사람)을 한꺼번에 부정했다: 둘이 **같은 편**에 어깨를 붙이고 앉아 있고 프레임 끝에 복제된 얼굴이 하나 더 있었다. 동작 문장을 마주 앉은 구도로 고쳐 4장을 새로 구웠지만(2장 + 의상 정합 후 2장) 전부 두 번째 인물을 배경으로 밀거나 주인공을 복제했다 — 이 구도는 **시드가 정한다**(실측: 기본 프롬프트에서도 3장 중 1장). 이긴 것은 이미 등록돼 있던 `cf_ed1729_2` 다: 가방을 메고 일어선 그녀, 돌아앉아 얼굴이 안 보이는 그, 식은 잔. 저장된 프롬프트는 그 컷의 레시피와 바이트 단위로 같다 |
| 143 | 저장된 프롬프트 ↔ 지금의 조립부(`prompt-drift` 자문) | 조립 규칙은 실측마다 바뀌는데 프롬프트는 장면 파일에 **저장**된다. 그래서 옛 컷의 프롬프트는 옛 규칙으로 남는데(005·006·011 의 `sunset`, 009 의 `couple`), 그것을 아무도 말해 주지 않았다 — 지난 주기의 실측이 우연히 발견했을 뿐이다. '틀렸다' 가 아니라 '갈라졌다' 를 말한다(그 그림은 그 조리법으로 구워졌다). 동작 문장은 비교하지 않는다. 남은 세 장은 **둘 다 그대로 두기로** 했다: 하늘 띠를 재니 파랑우세 .039~.237 로 전부 앨범의 노을 대역 안이고(golden hour 로 구운 012 가 .325 로 오히려 더 파랗다), 셋 다 하늘이 좁은 컷(투샷·클로즈업)이라 그 낱말이 고치려던 실패 모드에 애초에 걸리지 않았다 |
| 144 | 다시 만들 수 있는 것에만 보관 규칙을 준다 | `output/print` 239.1MB(47개) · `output/.cache` 60.2MB(61개) · `output/.thumbs` 1.7MB(85개). 셋 다 승인된 컷에서 다시 만들어지는데 규칙이 있는 것은 캐시 하나뿐이었고 그마저 **개수**(160개)라 실제로는 160MB 를 허용했다 — 항목 하나가 컷 한 장을 담은 data URI 다. 썸네일은 지우는 코드가 아예 없었다. 인화 마스터는 규격 폴더 최근 3종만 남기고(방금 구운 규격과 사람이 만든 폴더는 건드리지 않는다), 캐시·썸네일은 바이트 예산(64MB · 32MB)으로 줄인다. **후보 이미지(115.8MB)와 백업(307.5MB)은 도구가 지우지 않는다** — 다시 만들 수 없거나 사람이 고를 문제다. 대신 doctor 에 `[용량]` 절을 두어 크기와 '누가 지워도 되는가'를 한자리에 보여 준다 |
| 145 | 대사창·툴바의 폭 기준은 창이 아니라 **그림**이다 | 컷은 2:3 세로인데 창은 대개 가로로 넓다 — 그림은 가운데 맞춤이라 양옆이 레터박스인데 대사창·툴바는 창을 기준으로 놓여 있었다. 실측: 1280×900 에서 그림 600px 인데 대사창 880px(양옆 140px 씩 넘침), 툴바 오른끝이 그림보다 **326px 바깥**(1600×1000 에서는 453px). JS 가 그려진 폭을 재서 `--vnr-picw`·`--vnr-gut` 로 넣고 셋이 그 값을 본다. 폭만 줄이면 툴바가 두세 줄로 접혀 그림을 덮으므로, 폰에서 쓰던 `[⋯]` 장치를 데스크톱에서도 쓴다(버튼 노드를 옮길 뿐 새로 만들지 않는다). 덤으로 `[자동]` 뒤에 깔려 읽히지 않던 ♥ 눈금이 드러났다 |
| 146 | SCENE-002 — 대사의 음료와 그림의 음료가 달랐다 | 대사는 '딸기라떼', 동작 문장은 'sharing warm drinks', 그림은 키 큰 **아이스티 한 잔**이고 그걸 남자가 빨대로 마시고 있다. 재렌더 없이 글자만 고쳤다 — 대사를 "네가 골라준 아이스티, 역시 맛있네. 한 모금 마셔 볼래?" 로 바꾸니 '남자가 빨대를 문 채 얼굴이 붉어진' 그림이 그 한 줄로 설명된다. 저장된 프롬프트는 건드리지 않았다(그림을 안 굽는 한 조리법도 그대로 둔다 — 143) |
| 147 | 문서가 사실이 아닌 것을 말하던 세 자리 | ① 복구 런북이 "코드는 git 이 없으면 백업 사본에서" 라고 했는데 `project_*.zip` 에는 `tools/` 가 한 줄도 없다(`_zip_payload`). ② 복원 직후 `verify` 의 `✗ 누락` 을 "그 스냅샷에 이미지가 없었던 것" 이라 설명했는데, 실제 누락은 `_gen_meta.json`(장면당 1개)이고 이미지는 멀쩡히 돌아와 있다 — 같은 스냅샷을 다시 restore 하라는 처방은 무한 루프였다. ③ 인화 가이드가 4×6·5×7 을 단서 없이 권했는데 같은 문서 기준으로 208DPI·166DPI = "뽑으면 뭉개진다" 다. ④ 프라이버시 문서가 MakeFun 단독 시절 그대로였다 — 기본 엔진이 로컬 ComfyUI 라 **아무것도 나가지 않는데** 프롬프트가 나간다고 적혀 있었고, 정작 진짜 노출 둘(원격 `COMFYUI_URL` · 백업 `--dest` 가 채팅 로그까지 복사)은 없었다 |
| 148 | (사람 몫) 코드가 아무 데도 백업되지 않는다 | `git rev-list --count origin/main..HEAD` 로 **세어서 본다**(2026-09-15 검증 시점 60 — 박아 둔 숫자는 커밋마다 낡는다), origin/main 은 2026-08-25 이고 **ComfyUI 엔진 파일 셋**(`comfyui_client`·`gen_common`·`image_gen`)이 통째로 빠져 있다. 백업 zip 에는 코드가 없으므로(147-①) 지금 디스크가 죽으면 되살아나는 저장소는 그림을 한 장도 못 만든다. 저장소 규약이 push 를 금지하므로 **사용자가 직접** 밀어야 한다. 런북과 doctor 안내는 147 에서 고쳐 두었다 |
